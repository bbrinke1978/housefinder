---
phase: 19-wholesale-leads
plan: 04
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - app/src/lib/wholesale-actions.ts
  - app/src/components/wholesale-detail-header.tsx
  - app/src/components/wholesaler-directory.tsx
  - app/src/app/(dashboard)/wholesale/page.tsx
  - app/src/components/app-sidebar.tsx
  - app/src/components/command-menu.tsx
  - app/src/components/deal-card.tsx
autonomous: true
requirements:
  - WHOLESALE-07
  - WHOLESALE-09
  - WHOLESALE-12

must_haves:
  truths:
    - "User can promote a wholesale lead to a Deal with all numbers pre-filled and deal tagged as wholesale-sourced"
    - "User can view a wholesaler directory showing aggregate stats (deals sent, promoted, avg spread)"
    - "Wholesale appears in the sidebar navigation and command menu"
    - "Promoted deals show a 'Wholesale' badge on deal cards"
  artifacts:
    - path: "app/src/lib/wholesale-actions.ts"
      provides: "promoteToDeal server action"
      exports: ["promoteToDeal"]
    - path: "app/src/components/wholesaler-directory.tsx"
      provides: "Wholesaler list with stats table"
      min_lines: 40
    - path: "app/src/components/app-sidebar.tsx"
      provides: "Sidebar with Wholesale nav item"
      contains: "Wholesale"
    - path: "app/src/components/command-menu.tsx"
      provides: "Command menu with Wholesale navigation"
      contains: "Wholesale"
  key_links:
    - from: "app/src/lib/wholesale-actions.ts"
      to: "app/src/lib/deal-actions.ts"
      via: "promoteToDeal calls createDeal with pre-filled FormData"
      pattern: "createDeal"
    - from: "app/src/components/app-sidebar.tsx"
      to: "/wholesale"
      via: "navItems href"
      pattern: "wholesale"
    - from: "app/src/components/deal-card.tsx"
      to: "deals.leadSource"
      via: "Wholesale badge when leadSource === wholesale"
      pattern: "leadSource.*wholesale"
---

<objective>
Wire the "Promote to Deal" action, build the wholesaler directory, update navigation, and add wholesale badge to deal cards.

Purpose: Complete the wholesale lead lifecycle -- leads can be promoted into the Deals pipeline, wholesaler performance is tracked, and the feature is discoverable via navigation.
Output: promoteToDeal server action, wholesaler directory with stats, sidebar + command menu navigation, wholesale badge on deal cards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-wholesale-leads/19-01-SUMMARY.md
@.planning/phases/19-wholesale-leads/19-02-SUMMARY.md
@.planning/phases/19-wholesale-leads/19-03-SUMMARY.md

@app/src/lib/wholesale-actions.ts
@app/src/lib/wholesale-queries.ts
@app/src/lib/deal-actions.ts
@app/src/components/app-sidebar.tsx
@app/src/components/command-menu.tsx
@app/src/components/deal-card.tsx
@app/src/components/wholesale-detail-header.tsx
@app/src/app/(dashboard)/wholesale/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote to Deal action, wholesaler directory, and detail header wiring</name>
  <files>app/src/lib/wholesale-actions.ts, app/src/components/wholesale-detail-header.tsx, app/src/components/wholesaler-directory.tsx, app/src/app/(dashboard)/wholesale/page.tsx</files>
  <action>
**Add to wholesale-actions.ts** -- `promoteToDeal` server action:
- `export async function promoteToDeal(wholesaleLeadId: string): Promise<{ dealId: string }>`
- Auth check
- Fetch the wholesale lead via getWholesaleLead
- Build FormData for createDeal (from deal-actions.ts) with pre-filled fields:
  - address, city (from lead)
  - askingPrice, arv, repairEstimate (from lead)
  - wholesaleFee: "15000" (default)
  - sellerName: wholesaler name (the wholesaler is the "seller" in this context)
  - sellerPhone: wholesaler phone
- Call `createDeal(formData)` -- reuse existing action, do NOT duplicate logic
- After createDeal returns `{ id: dealId }`:
  - Update deals row: set `leadSource = "wholesale"` via `db.update(deals).set({ leadSource: "wholesale" }).where(eq(deals.id, dealId))`
  - Update wholesale lead: set `status = "promoted"`, `promotedDealId = dealId`
  - Auto-log a status_change note: "Promoted to Deal"
- revalidatePath("/wholesale") and revalidatePath("/deals")
- Return `{ dealId }`

**Update wholesale-detail-header.tsx:**
- Replace the disabled "Promote to Deal" button with a working one
- onClick calls promoteToDeal(lead.id) via useTransition
- On success: redirect to /deals/[dealId] using router.push
- Show loading state during promotion
- If lead.status === "promoted" and lead.promotedDealId exists: show "View Deal" link instead of promote button
- Promote button only visible when status is NOT "pass" and NOT "promoted"

**wholesaler-directory.tsx** -- "use client" component:
- Props: `{ wholesalers: WholesalerWithStats[] }`
- Table layout (shadcn Table):
  - Columns: Name, Company, Email, Phone, Deals Sent, Deals Promoted, Avg Spread, Status
  - Deals Sent = totalSent count
  - Deals Promoted = totalPromoted count with conversion rate (promoted/sent %)
  - Avg Spread = formatted dollar amount (or "N/A" if null)
  - Status: Active/Inactive badge
- Sort by totalSent descending (best sources first)
- Empty state: "No wholesalers tracked yet. They'll appear here as deals come in."
- Clicking wholesaler name could link to filtered /wholesale?wholesaler=id (optional, use URL param)

**Update wholesale/page.tsx:**
- Add a tab or section toggle: "Leads" (default) and "Wholesalers" (directory)
- Use simple client-side tab state (useState, two buttons)
- "Leads" tab shows WholesaleLeadGrid (existing)
- "Wholesalers" tab shows WholesalerDirectory
- Fetch `getWholesalersWithStats()` alongside existing queries in Promise.all
- Pass wholesalersWithStats to WholesalerDirectory
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Promote to Deal creates a deal pre-filled from wholesale lead, tags it as wholesale-sourced, and redirects. Wholesaler directory shows aggregate stats table. /wholesale page has Leads/Wholesalers tab toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation updates and wholesale badge on deal cards</name>
  <files>app/src/components/app-sidebar.tsx, app/src/components/command-menu.tsx, app/src/components/deal-card.tsx</files>
  <action>
**app-sidebar.tsx:**
- Add Wholesale to navItems array: `{ label: "Wholesale", href: "/wholesale", icon: Store }` (import Store from lucide-react)
- Position: after Buyers, before Analytics. This groups deal-related pages together (Deals, Contracts, Buyers, Wholesale).
- Do NOT add to bottom-nav.tsx (5-slot constraint, per research recommendation)

**command-menu.tsx:**
- Add to navigationItems: `{ label: "Wholesale", href: "/wholesale", icon: Store, description: "Triage wholesale deals" }`
- Position: after Buyers entry

**deal-card.tsx:**
- Check if the deal has `leadSource === "wholesale"` (the deals list query should already include leadSource since it's on the deals table)
- If so, show a small "Wholesale" badge in the card header area, using the badge tinting pattern: `bg-amber-500/10 text-amber-700 border-amber-500/20 dark:text-amber-400` with a Store icon
- This requires passing `leadSource` through to the card component. Check the existing DealWithBuyer type or deal query -- if leadSource is not already in the type, add it to the type in types/index.ts and the query in deal-queries.ts (getDealsList or equivalent)
- Badge is small/subtle -- should not compete with the deal status badge
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Sidebar shows Wholesale link with Store icon. Command menu includes Wholesale navigation. Deal cards show "Wholesale" badge when deal was promoted from wholesale lead.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- `cd app && npx next build` succeeds
- Sidebar shows Wholesale between Buyers and Analytics
- Command menu (Ctrl+K) shows Wholesale option
- Bottom nav unchanged (still 5 items)
- promoteToDeal creates a deal and sets leadSource = "wholesale"
- Deal cards with leadSource "wholesale" show a subtle badge
- Wholesaler directory shows stats table
</verification>

<success_criteria>
- Promote to Deal flow works end-to-end: wholesale lead -> creates Deal -> redirects to deal detail
- Promoted deals are tagged with leadSource "wholesale"
- Deal cards show "Wholesale" badge for promoted deals
- Wholesaler directory shows deals sent, promoted, conversion rate, avg spread
- Wholesale appears in sidebar and command menu
- Bottom nav unchanged (5 slots preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/19-wholesale-leads/19-04-SUMMARY.md`
</output>
