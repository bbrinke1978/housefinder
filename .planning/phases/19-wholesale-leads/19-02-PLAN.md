---
phase: 19-wholesale-leads
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - app/src/app/(dashboard)/wholesale/page.tsx
  - app/src/components/wholesale-lead-card.tsx
  - app/src/components/wholesale-lead-grid.tsx
  - app/src/components/wholesale-lead-form.tsx
  - app/src/components/wholesale-analysis.tsx
autonomous: true
requirements:
  - WHOLESALE-01
  - WHOLESALE-03
  - WHOLESALE-04
  - WHOLESALE-05
  - WHOLESALE-10
  - WHOLESALE-11

must_haves:
  truths:
    - "User can view a card grid of wholesale leads with address, asking/ARV, traffic light verdict badge, profit estimate, and wholesaler name"
    - "User can filter wholesale leads by verdict (green/yellow/red), status (new/analyzing/interested/pass/promoted), and wholesaler"
    - "User can manually enter a new wholesale lead with all deal fields and see auto-computed analysis on save"
    - "Traffic light badge shows green/yellow/red with expandable score breakdown"
  artifacts:
    - path: "app/src/app/(dashboard)/wholesale/page.tsx"
      provides: "/wholesale list page (server component)"
      min_lines: 30
    - path: "app/src/components/wholesale-lead-card.tsx"
      provides: "Card showing address, asking/ARV, verdict badge, profit, wholesaler"
      min_lines: 40
    - path: "app/src/components/wholesale-lead-grid.tsx"
      provides: "Responsive grid + filter bar"
      min_lines: 50
    - path: "app/src/components/wholesale-lead-form.tsx"
      provides: "Manual entry form with all deal fields"
      min_lines: 80
    - path: "app/src/components/wholesale-analysis.tsx"
      provides: "Traffic light + score + expandable breakdown"
      min_lines: 40
  key_links:
    - from: "app/src/app/(dashboard)/wholesale/page.tsx"
      to: "app/src/lib/wholesale-queries.ts"
      via: "getWholesaleLeads() server fetch"
      pattern: "getWholesaleLeads"
    - from: "app/src/components/wholesale-lead-form.tsx"
      to: "app/src/lib/wholesale-actions.ts"
      via: "createWholesaleLead server action"
      pattern: "createWholesaleLead"
    - from: "app/src/components/wholesale-analysis.tsx"
      to: "app/src/lib/wholesale-score.ts"
      via: "computeWholesaleScore client-side call"
      pattern: "computeWholesaleScore"
---

<objective>
Build the /wholesale list page with card grid layout, filter bar, manual entry form, and analysis display components.

Purpose: The primary UI for wholesale lead triage -- the user lands here to scan incoming deals, filter by verdict/status, and manually add new leads.
Output: Server-rendered /wholesale page, card grid with verdict badges, filter bar, manual entry form with auto-analysis.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-wholesale-leads/19-01-SUMMARY.md

@app/src/lib/wholesale-score.ts
@app/src/lib/wholesale-actions.ts
@app/src/lib/wholesale-queries.ts
@app/src/types/index.ts
@app/src/components/deal-card.tsx
@app/src/app/(dashboard)/deals/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale lead card, analysis display, and grid with filters</name>
  <files>app/src/components/wholesale-lead-card.tsx, app/src/components/wholesale-analysis.tsx, app/src/components/wholesale-lead-grid.tsx</files>
  <action>
**wholesale-lead-card.tsx** -- "use client" component:
- Props: `WholesaleLeadWithWholesaler` + `onClick?: () => void`
- Card layout (shadcn Card) showing:
  - Address as title (bold, truncated)
  - City/State below address in muted text
  - Asking price and ARV side by side: "Asking: $169K | ARV: $325K" (format with Intl.NumberFormat compact)
  - VerdictBadge component inline: green "Strong Deal" / yellow "Marginal" / red "Pass" with colored dot
  - Profit estimate in large bold text: "$XX,XXX" = MAO - askingPrice. Green text if positive, red if negative.
  - Deal score "7/10" small text next to verdict badge
  - Wholesaler name at bottom in muted text (or "Manual entry" if no wholesaler)
  - Status badge in top-right corner (same tinting pattern as Phase 17-03: bg-primary/10 etc)
- Link wrapping: entire card is clickable, navigates to /wholesale/[id]
- Mobile: full width. Desktop: card in grid.

**wholesale-analysis.tsx** -- "use client" component:
- Props: `{ arv, repairEstimate, askingPrice, wholesaleFee?, className? }`
- Calls `computeWholesaleScore()` client-side from wholesale-score.ts
- Displays:
  - Traffic light: 3 circles (green/yellow/red) with active one filled, others outlined
  - Score: "7/10" in large text next to traffic light
  - Profit estimate: big dollar amount "Your Spread: $XX,XXX" (spreadDollars from breakdown)
  - MAO value: "MAO: $XXX,XXX"
  - Expandable breakdown (details/summary or collapsible div):
    - "MAO Spread: X/10" with progress bar
    - "Equity %: X/10" with progress bar
    - "End Buyer ROI: X/10" with progress bar
    - End buyer ROI percentage shown
- Returns null if arv or askingPrice missing (insufficient data)
- Pure client-side computation, no fetch

**wholesale-lead-grid.tsx** -- "use client" component:
- Props: `{ leads: WholesaleLeadWithWholesaler[], wholesalers: { id: string, name: string }[] }`
- Filter bar at top with:
  - Verdict filter: "All" / "Green" / "Yellow" / "Red" buttons (inline toggle group)
  - Status filter: select dropdown with New/Analyzing/Interested/Pass/Promoted options
  - Wholesaler filter: select dropdown populated from wholesalers prop
  - "Add Lead" button (opens manual entry form via Dialog)
- Client-side filtering (no server roundtrip) -- filter leads array in state
- Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Renders WholesaleLeadCard for each filtered lead
- Empty state: "No wholesale leads yet. Forward a deal email or add one manually." with CTA button
- Count indicator: "Showing X of Y leads"
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Card shows address, prices, verdict badge, profit, and wholesaler. Grid renders responsive card layout with working client-side verdict/status/wholesaler filters. Analysis component shows traffic light, score, profit, and expandable breakdown.</done>
</task>

<task type="auto">
  <name>Task 2: Manual entry form and /wholesale list page</name>
  <files>app/src/components/wholesale-lead-form.tsx, app/src/app/(dashboard)/wholesale/page.tsx</files>
  <action>
**wholesale-lead-form.tsx** -- "use client" component:
- Rendered inside a Dialog (opened by "Add Lead" button in grid)
- Form fields matching CONTEXT.md manual form spec:
  - Address (required), City, State (default "UT"), Zip
  - Asking Price, ARV, Repair Estimate (all integer inputs with $ prefix)
  - Sqft, Beds, Baths, Lot Size, Year Built
  - Wholesaler: name, phone, email (grouped section)
  - Source Channel: select with email/social/text options
  - Notes: textarea
- Live analysis preview: as user types ARV, asking price, repair estimate, show WholesaleAnalysis component below the form updating in real-time (use controlled state, call computeWholesaleScore on change)
- Submit calls `createWholesaleLead` server action via useTransition
- On success: close dialog, revalidate page
- Validation: address required, prices must be positive integers when provided

**wholesale/page.tsx** -- Server component:
- Fetch data: `getWholesaleLeads()` and `getWholesalers()` in parallel via Promise.all
- Page header: "Wholesale Leads" title with subtitle "Triage incoming wholesale deals"
- Render WholesaleLeadGrid with leads and wholesalers props
- Follow existing page patterns (see deals/page.tsx for reference)
- Export `const dynamic = "force-dynamic"` to prevent build-time DB queries (Phase 02-05 pattern)
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>/wholesale page renders card grid with filters. Manual entry form opens in dialog, shows live analysis preview, submits to server action. Page uses force-dynamic export.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- `cd app && npx next build` succeeds
- /wholesale route exists in build output
- Card grid shows empty state when no leads exist
- Manual entry form has all required fields (address, asking, ARV, repairs, wholesaler info, source channel)
- Live analysis preview updates as user types prices
</verification>

<success_criteria>
- /wholesale page renders with card grid layout
- Cards show address, asking/ARV, traffic light verdict, profit estimate, wholesaler name
- Filters work client-side for verdict, status, and wholesaler
- Manual entry form captures all deal fields with live analysis preview
- Form submission creates a wholesale lead with auto-computed score
</success_criteria>

<output>
After completion, create `.planning/phases/19-wholesale-leads/19-02-SUMMARY.md`
</output>
