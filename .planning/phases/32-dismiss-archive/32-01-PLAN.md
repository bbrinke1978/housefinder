---
phase: 32-dismiss-archive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0018_dismiss_archive.sql
  - app/scripts/migrate-0018-dismiss-archive.ts
  - app/src/db/schema.ts
  - scraper/src/db/schema.ts
  - app/src/lib/actions.ts
  - app/src/lib/deal-actions.ts
  - app/src/lib/queries.ts
  - app/src/lib/deal-queries.ts
  - scraper/src/lib/upsert.ts
  - app/src/components/dismiss-lead-modal.tsx
  - app/src/components/archive-deal-button.tsx
  - app/src/components/permanent-delete-modal.tsx
  - app/src/components/property-card.tsx
  - app/src/components/deal-overview.tsx
  - app/src/app/(dashboard)/properties/[id]/page.tsx
  - app/src/app/(dashboard)/page.tsx
  - app/src/app/(dashboard)/deals/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Schema applied: leads.dismissed_at + dismissed_by_user_id + dismissed_reason; deals.archived_at + archived_by_user_id + archived_reason; dismissed_parcels table (parcel_id PK)"
    - "Dashboard property cards have a × icon top-right that opens a Dismiss modal with required reason dropdown (wrong_owner / already_sold / not_in_target / duplicate / other)"
    - "Default dashboard query excludes leads where dismissed_at IS NOT NULL; a 'Show dismissed' toggle (URL ?show_dismissed=true) brings them back"
    - "When a lead is dismissed, the parcel_id is added to dismissed_parcels (idempotent INSERT ON CONFLICT DO NOTHING)"
    - "scraper/src/lib/upsert.ts upsertProperty checks dismissed_parcels before INSERT and skips suppressed parcels with a console log"
    - "Property detail header shows 'Dismiss this lead' button when not dismissed; shows 'Dismissed by {name} {timeAgo} — {reason}' + Un-dismiss button when dismissed"
    - "Deal detail has an 'Archive Deal' button in status controls; archived deals show a gray banner; default deals query excludes archived; 'Show archived' toggle brings them back"
    - "Owner-only Permanent Delete button on property and deal detail pages, behind a confirm modal that requires typing the address (case-insensitive match) before the Delete button enables"
    - "All dismiss / archive / un-dismiss / un-archive / hard-delete actions are audit-logged and gated by userCan checks"
    - "tsc --noEmit clean and next lint clean (no errors, warnings OK) in app/"
    - "/analytics/outreach Log-a-call form: Property/Lead dropdown is populated (was previously empty due to a bug), filters to deals NOT in closed/dead/archived statuses, supports typeahead search (combobox not plain select), and the Source field is removed since deals already carry a lead_source"
  artifacts:
    - path: "app/drizzle/0018_dismiss_archive.sql"
      provides: "DDL for new columns and dismissed_parcels table"
      contains: "dismissed_parcels"
    - path: "app/src/components/dismiss-lead-modal.tsx"
      provides: "Reusable dismiss modal with reason dropdown + notes"
      contains: "DismissLeadModal"
    - path: "app/src/components/archive-deal-button.tsx"
      provides: "Archive/un-archive control for deals"
      contains: "ArchiveDealButton"
    - path: "app/src/components/permanent-delete-modal.tsx"
      provides: "Owner-only confirm-by-typing-address modal"
      contains: "PermanentDeleteModal"
  key_links:
    - from: "scraper upsertProperty"
      to: "dismissed_parcels suppression list"
      via: "SELECT 1 FROM dismissed_parcels WHERE parcel_id = $1 — early-return if exists"
      pattern: "dismissed_parcels"
    - from: "Dashboard query"
      to: "leads.dismissed_at IS NULL filter"
      via: "WHERE clause; conditional based on show_dismissed param"
      pattern: "dismissed_at"
    - from: "Dismiss modal submit"
      to: "dismissLead server action + audit log"
      via: "userCan('lead.edit_status') gate, transaction sets dismissed_at + inserts dismissed_parcels row"
      pattern: "dismissLead"
---

<objective>
Ship dismiss-leads on dashboard + archive-deals + owner-only permanent-delete, all in one plan. Schema → server actions → modal/button components → integrate into property card / property detail / deal detail / dashboard / deals kanban.

5 tasks, executed in dependency order. Target ~2 hours.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/32-dismiss-archive/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration 0018</name>
  <files>
    app/drizzle/0018_dismiss_archive.sql
    app/scripts/migrate-0018-dismiss-archive.ts
    app/src/db/schema.ts
    scraper/src/db/schema.ts
  </files>
  <action>
SQL:
```sql
ALTER TABLE leads
  ADD COLUMN IF NOT EXISTS dismissed_at timestamptz,
  ADD COLUMN IF NOT EXISTS dismissed_by_user_id uuid REFERENCES users(id),
  ADD COLUMN IF NOT EXISTS dismissed_reason text,
  ADD COLUMN IF NOT EXISTS dismissed_notes text;
CREATE INDEX IF NOT EXISTS idx_leads_dismissed_at ON leads (dismissed_at) WHERE dismissed_at IS NOT NULL;

ALTER TABLE deals
  ADD COLUMN IF NOT EXISTS archived_at timestamptz,
  ADD COLUMN IF NOT EXISTS archived_by_user_id uuid REFERENCES users(id),
  ADD COLUMN IF NOT EXISTS archived_reason text;
CREATE INDEX IF NOT EXISTS idx_deals_archived_at ON deals (archived_at) WHERE archived_at IS NOT NULL;

CREATE TABLE IF NOT EXISTS dismissed_parcels (
  parcel_id text PRIMARY KEY,
  dismissed_by_user_id uuid REFERENCES users(id),
  dismissed_at timestamptz NOT NULL DEFAULT now(),
  reason text NOT NULL,
  notes text
);
```

Drizzle: extend `leads`, `deals`, add new `dismissedParcels` table. Mirror in scraper schema (the scraper's `upsert.ts` queries this table).

Migration runner: mirror migrate-0017-activity.ts.
  </action>
  <verify>
    `cd app && npx tsc --noEmit && npx next lint` exit 0. Migration runner output shows new columns + table.
  </verify>
  <done>
    Schema in place, both app and scraper compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server actions for dismiss/archive/permanent-delete</name>
  <files>
    app/src/lib/actions.ts
    app/src/lib/deal-actions.ts
  </files>
  <action>
**actions.ts** add:
- `dismissLead(leadId, reason, notes?)` — gate `lead.edit_status` (Sales additionally must satisfy `canEditLead`). Transaction: UPDATE leads SET dismissed_at, dismissed_by_user_id, dismissed_reason, dismissed_notes; INSERT INTO dismissed_parcels ON CONFLICT DO NOTHING. logAudit action='lead.dismissed' with reason. revalidatePath('/').
- `undismissLead(leadId)` — same gate. UPDATE clears dismissed_*; DELETE from dismissed_parcels. logAudit action='lead.undismissed'.
- `permanentDeleteLead(leadId, addressConfirmation)` — gate `user.manage` (owner only). Verify addressConfirmation case-insensitive matches the property's address (or parcel_id if address NULL). Hard DELETE via existing cascade chain (extend the existing deleteLead's child-table sweep). logAudit action='lead.hard_deleted'. After delete keep dismissed_parcels entry.

**deal-actions.ts** add:
- `archiveDeal(dealId, reason?)` — gate `deal.edit_terms`. UPDATE deals SET archived_at, archived_by_user_id, archived_reason. logAudit action='deal.archived'. revalidatePath('/deals'), revalidatePath('/deals/[id]').
- `unarchiveDeal(dealId)` — same gate. Clear archived_*. logAudit.
- `permanentDeleteDeal(dealId, addressConfirmation)` — gate `user.manage`. Address-match guard. Hard DELETE deal + cascade child rows (deal_notes, deal_contracts, etc.). logAudit.

All actions revalidate the relevant paths so dashboard and detail pages update immediately.
  </action>
  <verify>
    `cd app && npx tsc --noEmit && npx next lint` exit 0. Manual smoke: call dismissLead from a test scratch script — leads row updated, dismissed_parcels row inserted, audit row appears.
  </verify>
  <done>
    Backend write paths in place.
  </done>
</task>

<task type="auto">
  <name>Task 3: Read-side filtering + scraper suppression</name>
  <files>
    app/src/lib/queries.ts
    app/src/lib/deal-queries.ts
    scraper/src/lib/upsert.ts
  </files>
  <action>
**queries.ts** `getProperties()`: add WHERE clause `leads.dismissed_at IS NULL` by default. Accept new filter param `showDismissed?: boolean`; when true, omit the filter. Same for `getDashboardStats()`.

**deal-queries.ts** `getDeals()`: add WHERE `deals.archived_at IS NULL` by default. Accept `showArchived?: boolean`.

**scraper upsert.ts upsertProperty()** at the top, after parcelId is normalized but before any DB writes:
```typescript
const [suppressed] = await db
  .select({ parcelId: dismissedParcels.parcelId })
  .from(dismissedParcels)
  .where(eq(dismissedParcels.parcelId, record.parcelId))
  .limit(1);
if (suppressed) {
  console.log(`[upsert] Skipping suppressed parcel ${record.parcelId}`);
  return null; // existing callers should handle null returns
}
```

Audit upsertProperty's existing callers (upsertFromAssessor, upsertFromDelinquent, upsertFromRecorder, etc.) — if any assume non-null return, fix the call site to handle null gracefully (just skip the signal-insert step too).
  </action>
  <verify>
    Dashboard with no `?show_dismissed=true` returns only non-dismissed leads. Manually test: add a row to dismissed_parcels for a known parcel; trigger a re-scrape (or call upsertProperty manually); confirm console log "Skipping suppressed parcel" and no DB write.
  </verify>
  <done>
    Read-side scoping + scrape-side suppression both functional.
  </done>
</task>

<task type="auto">
  <name>Task 4: UI components — dismiss / archive / permanent-delete</name>
  <files>
    app/src/components/dismiss-lead-modal.tsx
    app/src/components/archive-deal-button.tsx
    app/src/components/permanent-delete-modal.tsx
  </files>
  <action>
**DismissLeadModal:** open/close props, leadId, propertyAddress (for display). Reason dropdown with 5 options + notes textarea. Notes required when reason='other'. Submit calls `dismissLead`. Use @base-ui/react/dialog (matching Phase 28 pattern).

**ArchiveDealButton:** small button + inline confirm popover. "Archive Deal" → click → popover "Are you sure?" + optional reason input → "Archive" or "Cancel". Submit calls `archiveDeal`. Variant prop: 'archive' or 'unarchive' depending on deal state.

**PermanentDeleteModal:** Owner-only modal. Two-step: warning screen explaining what gets deleted (cascade list), then a confirm-by-typing-address screen. Delete button disabled until typed address matches (case-insensitive, whitespace-trimmed). Submit calls `permanentDeleteLead` or `permanentDeleteDeal` depending on entity prop.

All three components are reusable from any caller; consumers manage open state.
  </action>
  <verify>
    `cd app && npx tsc --noEmit && npx next lint` exit 0. Components standalone-render with mock props.
  </verify>
  <done>
    Reusable controls.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire into property card, property detail, deal detail, dashboard, deals kanban</name>
  <files>
    app/src/components/property-card.tsx
    app/src/components/deal-overview.tsx
    app/src/app/(dashboard)/properties/[id]/page.tsx
    app/src/app/(dashboard)/page.tsx
    app/src/app/(dashboard)/deals/page.tsx
  </files>
  <action>
**property-card.tsx:**
- Add a × icon (or 3-dot menu) at top-right of the card. Click → opens DismissLeadModal.
- If lead is already dismissed: show a thin gray "Dismissed" ribbon overlay instead of the dismiss button.
- Owner-only: a "Permanently delete" link in the same menu (or shown on hover for owner users).

**Dashboard page (`/(dashboard)/page.tsx`):**
- Read `?show_dismissed=true` from search params.
- Pass to `getProperties({ showDismissed })`.
- Add a small "Show dismissed" toggle chip near the existing filter row.

**Property detail (`/properties/[id]/page.tsx`):**
- Add Dismiss / Un-dismiss / Permanent-delete controls in the header next to existing controls (Hot Lead badge area). Only show controls user has permission for.
- If dismissed: show a gray banner at top — "Dismissed by {name} {timeAgo}: {reason}" with un-dismiss button.

**Deal detail (`deal-overview.tsx`):**
- Add ArchiveDealButton next to status controls.
- If archived: gray banner at top — "Archived by {name} {timeAgo}: {reason}" with unarchive button.
- Owner-only: PermanentDeleteModal trigger.

**Deals page (`/(dashboard)/deals/page.tsx`):**
- Read `?show_archived=true` from search params.
- Pass to `getDeals({ showArchived })`.
- Add "Show archived" toggle chip.
  </action>
  <verify>
    `cd app && npx tsc --noEmit && npx next lint` exit 0. Manual flow:
    1. Dashboard → click × on a card → modal opens → reason="not_in_target" → submit → card disappears
    2. Toggle "Show dismissed" → card returns with ribbon
    3. Click into dismissed property → see gray banner + Un-dismiss → click → goes back to normal
    4. As Owner: see Permanently delete link → opens modal → type wrong address → button disabled → type right address → button enables
    5. Same flow on a deal: archive → kanban hides it → "Show archived" toggle → returns
  </verify>
  <done>
    Full UI integration.
  </done>
</task>

<task type="auto">
  <name>Task 6: Fix /analytics/outreach Log-a-call form</name>
  <files>
    app/src/components/call-log-form.tsx
    app/src/lib/analytics-actions.ts
    app/src/app/(dashboard)/analytics/page.tsx
  </files>
  <action>
**Bug context:** The Log-a-call form on /analytics/outreach has an empty Property/Lead dropdown. The `leadsForForm` query in the analytics page is either broken or returning no rows, OR the form's select isn't binding to the data. Investigate and fix.

**Spec changes (per Brian 2026-05-01):**

1. **Scope to deals only**, NOT all leads. Replace the existing `leadsForForm` fetch with a new `getActiveDealsForCallForm()` query: SELECT deal.id, deal.address, deal.city, deal.sellerName, deal.propertyId, deal.lead_id (resolved via leads.property_id JOIN). WHERE deal.status NOT IN ('closed', 'dead') AND deal.archived_at IS NULL. ORDER BY deal.updatedAt DESC.

2. **Typeahead search.** Replace plain `<select>` with a combobox. Look at existing typeahead UX in the project first (buyer-list filter, deals new-page, etc.). If none exists, build a minimal one with @base-ui/react/popover + Input + filtered list. Match against deal.address + deal.sellerName + deal.city case-insensitively.

3. **Drop the Source field.** Deals already carry `lead_source` from the property's lead. Just write the contact_event with the deal's lead_id (existing pattern). Remove the Source select element.

4. **Outcome dropdown stays** — same options (answered / voicemail / no_answer / wrong_number / disconnected). On submit: INSERT INTO contact_events with `actor_user_id = session.user.id`, `event_type = 'called_client'`, `outcome = <selected>`, notes = (existing notes textarea).

After this task ships, /analytics/outreach Log-a-call works end-to-end: pick a deal via typeahead, pick outcome, submit, contact_event row appears, and the entry shows up in the unified activity feed for that deal.
  </action>
  <verify>
    `cd app && npx tsc --noEmit && npx next lint` exit 0. Manual: visit /analytics/outreach Outreach tab, dropdown is populated with active deals, typing filters the list, submit a test call, confirm row in contact_events with correct lead_id + actor_user_id + outcome.
  </verify>
  <done>
    Form works; matches the rest of the activity infrastructure; no source dropdown clutter.
  </done>
</task>

</tasks>

<verification>
End-to-end after the plan ships:

1. **Dismiss happy path:** card × → modal → reason → submit → card gone; "Show dismissed" toggle brings it back with ribbon; click → property detail shows gray banner; un-dismiss → restored.
2. **Archive happy path:** deal page → Archive button → optional reason → confirm → deal removed from kanban; "Show archived" toggle restores; un-archive button works.
3. **Re-scrape suppression:** dismiss a Carbon County parcel → wait for next nightly scrape (or manually invoke) → confirm dismissed_parcels still has the parcel and the lead row's dismissed_at didn't get re-cleared (i.e. upsert skipped, didn't recreate).
4. **Permanent delete (Owner only):** confirm modal requires typing address; wrong text leaves button disabled; correct text enables; delete cascades cleanly; audit log captures it; suppression list entry survives.
5. **Permission gates:** non-owner users (Stacee, Chris) can dismiss/un-dismiss leads but cannot see Permanent Delete UI; can archive deals only if they have deal.edit_terms.
6. **Audit log entries** for every action (dismiss, undismiss, archive, unarchive, hard_delete) appear in /admin/audit.
7. **Outreach form fix (Task 6):** /analytics/outreach Log-a-call dropdown populates with active deals (not in closed/dead/archived); typeahead works; Source field removed; submitting creates a contact_event row keyed to the deal's lead_id; row appears in the unified activity feed for that deal.

Phase 32 complete.
</verification>
