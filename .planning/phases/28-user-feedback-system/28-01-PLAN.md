---
phase: 28-user-feedback-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/db/schema.ts
  - app/drizzle/0015_feedback_system.sql
  - app/scripts/migrate-0015-feedback-system.ts
autonomous: true
requirements: [FB-01, FB-09, FB-10]

must_haves:
  truths:
    - "After running the migration, four new tables exist in production Postgres: feedback_items, feedback_comments, feedback_attachments, feedback_activity"
    - "Four new pgEnums exist: feedback_type, feedback_status, feedback_priority, feedback_activity_action"
    - "feedback_items has nullable property_id and deal_id FKs so context links work without forcing them"
    - "feedback_items has a GIN full-text-search index for title + description"
    - "All four tables have a deleted_at timestamp column for soft-delete"
    - "Drizzle types in app/src/db/schema.ts compile cleanly under tsc"
  artifacts:
    - path: "app/drizzle/0015_feedback_system.sql"
      provides: "DDL for four feedback tables, four enums, indexes"
      contains: "CREATE TABLE feedback_items"
    - path: "app/scripts/migrate-0015-feedback-system.ts"
      provides: "Idempotent migration runner — IF NOT EXISTS guards on tables/enums"
      contains: "0015_feedback_system.sql"
    - path: "app/src/db/schema.ts"
      provides: "Drizzle table definitions for the four feedback tables"
      contains: "feedbackItems"
  key_links:
    - from: "feedback_items.reporter_id / assignee_id"
      to: "users.id"
      via: "FK references"
      pattern: "REFERENCES users(id)"
    - from: "feedback_items.property_id"
      to: "properties.id"
      via: "nullable FK"
      pattern: "REFERENCES properties(id)"
    - from: "feedback_attachments.item_id / comment_id"
      to: "feedback_items.id / feedback_comments.id"
      via: "nullable FKs (CHECK enforces at least one)"
      pattern: "CHECK ((item_id IS NOT NULL) OR (comment_id IS NOT NULL))"
---

<objective>
Add the four `feedback_*` tables and their enums to the schema, write the SQL migration file, and create the migration runner script. No app-level code yet — just persistence groundwork.

This plan is foundational. All other Phase 28 plans depend on the tables existing. After this plan runs, the schema is in place and ready for the backend (Plan 02) and UI (Plans 03–05).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-user-feedback-system/28-CONTEXT.md
@.planning/phases/28-user-feedback-system/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SQL migration</name>
  <files>app/drizzle/0015_feedback_system.sql</files>
  <action>
Create the migration SQL file at `app/drizzle/0015_feedback_system.sql` containing:
1. The four pgEnums (`feedback_type`, `feedback_status`, `feedback_priority`, `feedback_activity_action`) — wrap each `CREATE TYPE` in a DO block with `IF NOT EXISTS` semantics (Postgres < 17 doesn't support `CREATE TYPE IF NOT EXISTS` directly).
2. The four tables (`feedback_items`, `feedback_comments`, `feedback_attachments`, `feedback_activity`) using `CREATE TABLE IF NOT EXISTS`.
3. All indexes from RESEARCH.md section 1, including the GIN FTS index on title + description.
4. The `CHECK ((item_id IS NOT NULL) OR (comment_id IS NOT NULL))` constraint on `feedback_attachments`.

Mirror the SQL style of the existing `app/drizzle/0014_owner_mailing_address.sql`. Include a top-of-file comment explaining the phase context and reversibility guarantees (additive only — drops nothing).

Use this DO-block pattern for enum creation:
```sql
DO $$ BEGIN
  CREATE TYPE feedback_type AS ENUM ('bug', 'feature', 'idea', 'question');
EXCEPTION
  WHEN duplicate_object THEN null;
END $$;
```
  </action>
  <verify>
    File exists at `app/drizzle/0015_feedback_system.sql` and contains: `CREATE TABLE IF NOT EXISTS feedback_items`, `CREATE TABLE IF NOT EXISTS feedback_comments`, `CREATE TABLE IF NOT EXISTS feedback_attachments`, `CREATE TABLE IF NOT EXISTS feedback_activity`, all four enums, and the GIN index. SQL parses (no `psql -d` needed; just visual review).
  </verify>
  <done>
    The migration SQL file exists with idempotent DDL. No schema changes applied yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the migration runner script</name>
  <files>app/scripts/migrate-0015-feedback-system.ts</files>
  <action>
Mirror `app/scripts/migrate-0014-owner-mailing.ts` exactly (it's the most recent migration runner). It loads `.env.local`, reads the SQL file, splits on `;\s*\n`, executes each statement in order, prints OK/FAIL per statement, and verifies the four tables exist via `information_schema.tables` at the end.

Verification query:
```sql
SELECT table_name
FROM information_schema.tables
WHERE table_name IN ('feedback_items', 'feedback_comments', 'feedback_attachments', 'feedback_activity')
ORDER BY table_name
```

Expected to print 4 rows.
  </action>
  <verify>
    `node -c app/scripts/migrate-0015-feedback-system.ts` parses (or `npx tsc --noEmit` clean). Manual run of `cd app && npx tsx scripts/migrate-0015-feedback-system.ts` against prod prints "Migration 0015 applied successfully" and shows the 4 tables in the verification table.
  </verify>
  <done>
    Migration runner exists, runs cleanly, applies the schema. After execution, prod DB has the four tables and four enums.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Drizzle types to schema.ts</name>
  <files>app/src/db/schema.ts</files>
  <action>
Add the four pgEnums and four pgTables to `app/src/db/schema.ts`, matching the style of the existing `signalTypeEnum` / `signalStatusEnum` / `properties` / `distressSignals` patterns.

Place the new types just before the `// -- Wholesale Leads --` section (or wherever the file currently ends — append at bottom).

Use exactly the column types from RESEARCH.md section 1. Reference foreign keys via `.references(() => users.id)` etc. Add Drizzle indexes via `(table) => [...]` matching the SQL index names from the migration so Drizzle's introspection stays consistent.

For the `feedback_attachments` CHECK constraint (`item_id OR comment_id`), use Drizzle's `check` helper if available, or inline raw SQL: `check("attachments_target_check", sql\`item_id IS NOT NULL OR comment_id IS NOT NULL\`)`.

Run `npx tsc --noEmit` from `app/` after edits — should pass clean.
  </action>
  <verify>
    `cd app && npx tsc --noEmit` returns no errors. The four new exports `feedbackItems`, `feedbackComments`, `feedbackAttachments`, `feedbackActivity` are present in `schema.ts` and reference the correct enum types.
  </verify>
  <done>
    Drizzle schema reflects the new tables. App-side queries can now type-safely reference these tables in Plan 02.
  </done>
</task>

</tasks>

<verification>
Goal-backward verification:
1. **Tables exist in prod:** `SELECT count(*) FROM information_schema.tables WHERE table_name LIKE 'feedback_%'` returns 4.
2. **Enums exist:** `SELECT typname FROM pg_type WHERE typname LIKE 'feedback_%'` returns 4.
3. **FK reference users + properties + deals:** verified by enum-typed columns + INSERT test (deferred to Plan 02 testing).
4. **GIN index exists:** `SELECT indexname FROM pg_indexes WHERE indexname = 'idx_feedback_items_search'` returns 1 row.
5. **Drizzle types compile:** `npx tsc --noEmit` clean.

If any verification step fails, the migration is reversible by:
```sql
DROP TABLE IF EXISTS feedback_activity, feedback_attachments, feedback_comments, feedback_items CASCADE;
DROP TYPE IF EXISTS feedback_activity_action, feedback_priority, feedback_status, feedback_type;
```
</verification>
