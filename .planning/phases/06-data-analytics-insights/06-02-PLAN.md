---
phase: 06-data-analytics-insights
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/src/components/analytics-funnel.tsx
  - app/src/components/analytics-market.tsx
  - app/src/components/analytics-trends.tsx
  - app/src/components/analytics-attribution.tsx
  - app/src/app/(dashboard)/analytics/page.tsx
autonomous: true
requirements:
  - ANALYTICS-01
  - ANALYTICS-02
  - ANALYTICS-04
  - ANALYTICS-06

must_haves:
  truths:
    - "Pipeline tab shows a bar chart of lead counts per status stage with avg days at each stage"
    - "Markets tab shows grouped bars comparing cities by total leads, hot leads, and conversion rate"
    - "Trends tab shows a line chart of weekly distressed property volume per city over last 6 months"
    - "Attribution section shows which distress signal types produce the most deal conversions"
    - "All charts show helpful empty states when data is zero"
  artifacts:
    - path: "app/src/components/analytics-funnel.tsx"
      provides: "Pipeline conversion funnel bar chart"
      contains: "use client"
    - path: "app/src/components/analytics-market.tsx"
      provides: "Market comparison grouped bar chart"
      contains: "use client"
    - path: "app/src/components/analytics-trends.tsx"
      provides: "Property volume trend line chart"
      contains: "use client"
    - path: "app/src/components/analytics-attribution.tsx"
      provides: "Lead source attribution bar chart"
      contains: "use client"
  key_links:
    - from: "app/src/app/(dashboard)/analytics/page.tsx"
      to: "app/src/components/analytics-funnel.tsx"
      via: "import and render in pipeline tab"
      pattern: "AnalyticsFunnel"
    - from: "app/src/components/analytics-funnel.tsx"
      to: "recharts"
      via: "BarChart import"
      pattern: "import.*BarChart.*recharts"
---

<objective>
Build the four recharts-based chart components for pipeline funnel, market comparison, property trends, and lead source attribution, then wire them into the analytics page tabs.

Purpose: Transform raw analytics data into visual insights. These four charts cover the core "how is my business doing" questions: conversion rates, market performance, volume trends, and signal effectiveness.
Output: Four "use client" chart components rendering real data from analytics-queries.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-data-analytics-insights/06-RESEARCH.md
@.planning/phases/06-data-analytics-insights/06-01-SUMMARY.md

@app/src/lib/analytics-queries.ts
@app/src/app/(dashboard)/analytics/page.tsx

<interfaces>
<!-- Types from analytics-queries.ts created in Plan 01 -->

From app/src/lib/analytics-queries.ts:
```typescript
export interface FunnelStage {
  stage: string;
  count: number;
  avgDaysAtStage: number | null;
}

export interface MarketStat {
  city: string;
  totalLeads: number;
  hotLeads: number;
  conversionRate: number;
}

export interface TrendPoint {
  week: string;
  city: string;
  count: number;
}

export interface AttributionStat {
  signalType: string;
  totalLeads: number;
  convertedToDeals: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create funnel, market comparison, and attribution chart components</name>
  <files>
    app/src/components/analytics-funnel.tsx
    app/src/components/analytics-market.tsx
    app/src/components/analytics-attribution.tsx
  </files>
  <action>
All three components follow the same pattern: "use client", import from recharts, accept typed data prop, check for empty data and show encouraging empty state message, render chart inside ResponsiveContainer.

1. analytics-funnel.tsx (ANALYTICS-01):
   - Props: `{ data: FunnelStage[] }`
   - Empty state: "No pipeline data yet. Start tracking leads to see conversion rates."
   - Render a vertical BarChart with status stages on XAxis and count on YAxis.
   - Show avgDaysAtStage in the Tooltip (custom tooltip formatter showing "X leads, avg Y days").
   - Bar fill: use `hsl(var(--primary))` for brand consistency. Radius [4,4,0,0] for rounded tops.
   - Height: 300px via ResponsiveContainer.

2. analytics-market.tsx (ANALYTICS-02):
   - Props: `{ data: MarketStat[] }`
   - Empty state: "No market data yet. Properties from multiple cities will show comparison stats."
   - Render a grouped BarChart with city on XAxis, two Bars: totalLeads (muted color) and hotLeads (brand color).
   - Show conversionRate in Tooltip: "X total, Y hot, Z% conversion".
   - Height: 350px.

3. analytics-attribution.tsx (ANALYTICS-06):
   - Props: `{ data: AttributionStat[] }`
   - Empty state: "No attribution data yet. As leads convert to deals, signal effectiveness will appear."
   - Render a horizontal BarChart (layout="vertical") with signalType on YAxis, totalLeads bar and convertedToDeals bar stacked or side-by-side.
   - Format signal type labels: "nod" -> "NOD", "tax_lien" -> "Tax Lien", etc.
   - Height: 250px.

All charts: CartesianGrid with strokeDasharray="3 3", className="stroke-border". XAxis/YAxis tick fontSize 12. Tooltip with cursor false for cleaner look.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three chart components compile, accept typed props, render recharts charts with empty states</done>
</task>

<task type="auto">
  <name>Task 2: Create trends line chart and wire all charts into analytics page</name>
  <files>
    app/src/components/analytics-trends.tsx
    app/src/app/(dashboard)/analytics/page.tsx
  </files>
  <action>
1. analytics-trends.tsx (ANALYTICS-04):
   - Props: `{ data: TrendPoint[] }`
   - Empty state: "Not enough trend data yet. Weekly property volumes will appear after a few weeks of scraping."
   - Transform flat TrendPoint[] into recharts-compatible format: group by week, with each city as a separate key. Example: `[{ week: "2026-03-10", Price: 5, Ephraim: 2, ... }]`.
   - Render a LineChart with week on XAxis, one Line per city with different stroke colors. Use a color palette array (e.g., hsl brand shades or a simple set of 6-8 distinct colors).
   - XAxis: format week dates to "MMM D" using date-fns `format(new Date(week), "MMM d")`.
   - Legend at bottom showing city names.
   - Height: 350px.

2. Update analytics/page.tsx to replace placeholder content with real chart components:
   - "pipeline" tab: render `<AnalyticsFunnel data={funnelData} />` and below it `<AnalyticsAttribution data={attributionData} />` with a section heading "Lead Source Attribution" between them.
   - "markets" tab: render `<AnalyticsMarket data={marketData} />`
   - "trends" tab: render `<AnalyticsTrends data={trendData} />`
   - Import all four chart components at the top.
   - Wrap each tab's chart section in a card-like container: `<div className="rounded-xl border bg-card p-4 md:p-6">` for consistent styling.
   - Each section has a heading: e.g., "Pipeline Conversion" h2, "Lead Source Attribution" h3.
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All four chart components render in their respective tabs. Pipeline tab shows funnel + attribution. Markets tab shows city comparison. Trends tab shows weekly line chart. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd app && npx next build` succeeds
- All chart components are "use client" with recharts imports
- Empty states render when data arrays are empty/all-zero
- Pipeline tab shows both funnel and attribution charts
</verification>

<success_criteria>
- 4 chart components created: analytics-funnel, analytics-market, analytics-trends, analytics-attribution
- Each chart handles empty data gracefully with encouraging messages
- Analytics page pipeline tab shows funnel + attribution
- Analytics page markets tab shows market comparison
- Analytics page trends tab shows line chart
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-analytics-insights/06-02-SUMMARY.md`
</output>
