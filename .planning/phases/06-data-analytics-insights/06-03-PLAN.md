---
phase: 06-data-analytics-insights
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/src/components/analytics-scraper-health.tsx
  - app/src/components/analytics-outreach.tsx
  - app/src/components/call-log-form.tsx
  - app/src/lib/analytics-actions.ts
  - app/src/app/(dashboard)/analytics/page.tsx
autonomous: true
requirements:
  - ANALYTICS-03
  - ANALYTICS-05

must_haves:
  truths:
    - "Health tab shows per-county scraper status with red/yellow/green indicators and freshness hours"
    - "Outreach tab shows call outcome breakdown chart and a form to log new calls"
    - "User can log a call with outcome, source, duration, and notes from the outreach tab"
    - "Scraper health shows degrading source alerts (red status) for counties with 3+ consecutive zero results"
  artifacts:
    - path: "app/src/components/analytics-scraper-health.tsx"
      provides: "Scraper health status table with color indicators"
      contains: "green"
    - path: "app/src/components/analytics-outreach.tsx"
      provides: "Outreach stats chart"
      contains: "use client"
    - path: "app/src/components/call-log-form.tsx"
      provides: "Form to log call outcomes"
      contains: "use client"
    - path: "app/src/lib/analytics-actions.ts"
      provides: "Server action for creating call logs"
      exports: ["logCall"]
  key_links:
    - from: "app/src/components/call-log-form.tsx"
      to: "app/src/lib/analytics-actions.ts"
      via: "server action form submission"
      pattern: "logCall"
    - from: "app/src/app/(dashboard)/analytics/page.tsx"
      to: "app/src/components/analytics-scraper-health.tsx"
      via: "render in health tab"
      pattern: "ScraperHealthTable"
---

<objective>
Build the scraper health dashboard, outreach tracking UI with call log form, and wire them into the analytics page.

Purpose: ANALYTICS-05 gives visibility into data pipeline reliability (which scrapers are failing). ANALYTICS-03 tracks call activity so Brian can see what outreach is working. Together they answer "is my data fresh?" and "am I making enough calls?"
Output: Scraper health table with status indicators, outreach stats chart, call log form with server action.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-data-analytics-insights/06-RESEARCH.md
@.planning/phases/06-data-analytics-insights/06-01-SUMMARY.md

@app/src/lib/analytics-queries.ts
@app/src/lib/actions.ts
@app/src/db/schema.ts
@app/src/app/(dashboard)/analytics/page.tsx

<interfaces>
<!-- Types from analytics-queries.ts created in Plan 01 -->

From app/src/lib/analytics-queries.ts:
```typescript
export type HealthStatus = "green" | "yellow" | "red";

export interface ScraperHealthRow {
  county: string;
  lastRunAt: Date | null;
  lastSuccessAt: Date | null;
  lastResultCount: number;
  consecutiveZeroResults: number;
  status: HealthStatus;
  freshnessHours: number | null;
}

export interface OutreachStat {
  outcome: string;
  count: number;
  contactRate: number;
}
```

From app/src/db/schema.ts:
```typescript
export const callOutcomeEnum = pgEnum("call_outcome", [
  "answered", "voicemail", "no_answer", "wrong_number"
]);
export const callLogs = pgTable("call_logs", { ... });
```

From app/src/lib/queries.ts (existing pattern for lead lookup):
```typescript
// Leads are looked up by propertyId or leadId — call log form needs a lead selector
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scraper health table and outreach stats chart</name>
  <files>
    app/src/components/analytics-scraper-health.tsx
    app/src/components/analytics-outreach.tsx
  </files>
  <action>
1. analytics-scraper-health.tsx (ANALYTICS-05):
   - Props: `{ data: ScraperHealthRow[] }`
   - Render as a table (not a chart — tabular data is clearer for status monitoring).
   - Columns: County, Status (colored dot), Last Run, Last Success, Last Count, Zero Streak, Freshness.
   - Status dot: green = `bg-emerald-500`, yellow = `bg-amber-500`, red = `bg-red-500`. Render as `<span className="inline-block h-2.5 w-2.5 rounded-full {colorClass}" />`.
   - Format dates using date-fns `formatDistanceToNow(date, { addSuffix: true })` for "2 hours ago" style.
   - Freshness column: show hours, color text red if > 72h, amber if > 36h.
   - If consecutiveZeroResults >= 3, show a warning icon or text "ALERT" next to the county name.
   - Empty state: "No scraper health data available. Scrapers will report their status after their first run."
   - This can be a server component (no interactivity needed) or "use client" for date-fns — use "use client" since formatDistanceToNow needs the client's current time for accuracy.

2. analytics-outreach.tsx (ANALYTICS-03 chart portion):
   - Props: `{ data: OutreachStat[] }`
   - Empty state: "No call data yet. Log your first call to start tracking outreach effectiveness."
   - Render a horizontal BarChart (layout="vertical") with outcome on YAxis (formatted: "answered" -> "Answered", "no_answer" -> "No Answer", etc.) and count on XAxis.
   - Color bars by outcome: answered = emerald, voicemail = amber, no_answer = slate, wrong_number = red.
   - Show overall contact rate as a bold stat above the chart: "Contact Rate: X%" (answered / total * 100).
   - Height: 250px.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Scraper health table renders with color indicators. Outreach chart shows call outcome breakdown with contact rate stat.</done>
</task>

<task type="auto">
  <name>Task 2: Create call log form, server action, and wire health + outreach into analytics page</name>
  <files>
    app/src/lib/analytics-actions.ts
    app/src/components/call-log-form.tsx
    app/src/app/(dashboard)/analytics/page.tsx
  </files>
  <action>
1. Create app/src/lib/analytics-actions.ts:
   - "use server" directive.
   - `logCall(formData: FormData)` server action:
     - Extract: leadId (required), outcome (required — one of "answered", "voicemail", "no_answer", "wrong_number"), source (optional, default "manual"), durationSeconds (optional integer), notes (optional text).
     - Validate leadId is a valid UUID and outcome is one of the enum values.
     - Insert into callLogs table using `db.insert(callLogs).values({...})`.
     - Call `revalidatePath("/analytics")` to refresh the page data.
     - Return `{ success: true }` or `{ error: string }`.

2. Create app/src/components/call-log-form.tsx:
   - "use client" component.
   - Props: `{ leads: { id: string; address: string }[] }` — a simplified lead list for the dropdown.
   - Form with: lead select dropdown (searchable or just a <select> with lead address), outcome radio buttons or select (Answered, Voicemail, No Answer, Wrong Number), source text input (default "manual"), duration input (minutes, converted to seconds on submit), notes textarea.
   - Submit via form action calling logCall server action.
   - Show success/error feedback after submission using useActionState or simple state.
   - Style consistent with existing forms (see settings-form.tsx pattern — Tailwind classes, rounded-xl inputs).

3. Update analytics/page.tsx:
   - "health" tab: render `<ScraperHealthTable data={healthData} />`. Import component. Wrap in card container.
   - "outreach" tab: fetch outreach stats AND a simplified lead list for the form (query: `SELECT l.id, p.address FROM leads l JOIN properties p ON p.id = l.property_id ORDER BY p.address LIMIT 500`). Add this query as `getLeadsForCallLog()` to analytics-queries.ts. Render `<AnalyticsOutreach data={outreachData} />` and below it `<CallLogForm leads={leadsForForm} />` with a heading "Log a Call".
   - Import all three new components.
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Health tab shows scraper status table. Outreach tab shows call stats chart and call log form. Form submits and creates call_logs records. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd app && npx next build` succeeds
- Health tab renders scraper health table with colored status dots
- Outreach tab renders call outcome chart and call log form
- Call log form has lead dropdown, outcome selection, submit button
- analytics-actions.ts exports logCall server action
</verification>

<success_criteria>
- Scraper health table shows red/yellow/green per county with freshness and alert indicators
- Outreach chart shows call outcomes with contact rate
- Call log form allows logging calls with outcome, source, duration, notes
- Server action inserts into call_logs and revalidates the page
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-analytics-insights/06-03-SUMMARY.md`
</output>
