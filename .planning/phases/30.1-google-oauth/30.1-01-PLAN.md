---
phase: 30.1-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/auth.ts
  - app/src/middleware.ts
  - app/src/app/pending-approval/page.tsx
  - app/src/app/login/page.tsx
  - app/src/components/admin/users-table.tsx
  - app/.env.local.example
autonomous: true
requirements: []

must_haves:
  truths:
    - "auth.ts adds the Google provider alongside the existing Credentials provider; both work; tsc clean"
    - "Logging in via Google with an @no-bshomes.com email creates (or reuses) a users row, returns a session"
    - "Logging in via Google with a non-@no-bshomes.com email is rejected at the signIn callback (returns false → NextAuth shows access-denied page)"
    - "An auto-provisioned user (roles=[]) is redirected to /pending-approval on every protected route until Owner grants roles"
    - "An existing user (Brian/Shawn/admin@/Stacee) signs in via Google and reuses their existing users row — no duplicate created. Their roles + id flow through to session.user.roles + session.user.id."
    - "The login page shows the Google button on top, then a divider, then the existing email/password form. Both flows work."
    - "/admin/users surfaces pending users (roles=[]) with a yellow 'Pending' badge sorted to the top"
    - "Brian has the env var setup checklist documented in docs/google-services.md and the OAuth consent screen audience is set to 'Internal' in Cloud Console (a manual step Brian does outside this phase)"
  artifacts:
    - path: "app/src/auth.ts"
      provides: "NextAuth configured with Google + Credentials, signIn domain gate, jwt loads roles from DB"
      contains: "Google("
    - path: "app/src/middleware.ts"
      provides: "Empty-roles redirect to /pending-approval"
      contains: "pending-approval"
    - path: "app/src/app/pending-approval/page.tsx"
      provides: "Standalone page shown to users with no roles assigned"
      contains: "Pending Approval"
    - path: "app/src/app/login/page.tsx"
      provides: "Google sign-in button + existing email/password form"
      contains: "signIn(\"google\""
    - path: "app/src/components/admin/users-table.tsx"
      provides: "Pending badge + sort-pending-first for Owner triage"
      contains: "Pending"
  key_links:
    - from: "auth.ts signIn callback"
      to: "users table INSERT for new Google users"
      via: "email lookup, INSERT with empty roles + empty password_hash"
      pattern: "passwordHash: \"\""
    - from: "auth.ts jwt callback"
      to: "users.roles and users.id"
      via: "DB lookup on every initial sign-in (Google or Credentials)"
      pattern: "token.userId"
    - from: "middleware.ts"
      to: "/pending-approval page"
      via: "redirect when session roles array is empty"
      pattern: "NextResponse.redirect"
---

<objective>
Add "Sign in with Google" to /login. After this plan ships, new team members onboard by signing into their `@no-bshomes.com` Google Workspace account — no temp passwords, no admin-create-user.ts script. Existing users keep their email/password as a fallback.

Single plan. No waves. No external dependencies. The Cloud Console + Netlify env-var setup is a manual step Brian does in his browser per `docs/google-services.md` section 3 — it does NOT block this plan from being merged. The merged code waits for `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` to be set before Google sign-in works; until then, the email/password form continues to work normally.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/30.1-google-oauth/30.1-CONTEXT.md
@.planning/phases/30.1-google-oauth/30.1-RESEARCH.md
@docs/google-services.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google provider to auth.ts + signIn + jwt callbacks</name>
  <files>app/src/auth.ts</files>
  <action>
Per 30.1-RESEARCH.md "Code changes section 1" — verbatim. Add:

1. Import `Google` from `next-auth/providers/google`
2. Add Google provider to the `providers` array (alongside the existing Credentials provider — keep both)
3. Add a `signIn` callback that:
   - Returns true for the Credentials provider (existing behavior)
   - For the Google provider: lowercases the email; rejects if not ending `@no-bshomes.com`; looks up the user by email; INSERTs with empty roles + empty password_hash if not found; rejects if found but `is_active=false`; returns true otherwise
4. Update the `jwt` callback to load fresh roles + userId from the DB on every initial sign-in (the existing logic only ran on Credentials sign-in via the `user` parameter). The `account?.provider === "google"` branch handles the Google flow.
5. Keep the existing `session` callback unchanged.

Use dynamic imports inside the callbacks (matches the existing pattern in auth.ts to avoid Edge Runtime issues with pg/drizzle).

After this task, `npx tsc --noEmit` must pass. Don't commit yet if TS fails — fix the type errors first.
  </action>
  <verify>
    `cd app && npx tsc --noEmit` clean. Manual code review: the Google provider is in the array, signIn enforces the domain check + auto-provision, jwt reads from DB, session unchanged.
  </verify>
  <done>
    auth.ts compiles. The code is dormant until env vars are set; Credentials login still works as before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Middleware for pending-approval redirect</name>
  <files>app/src/middleware.ts</files>
  <action>
Check whether `app/src/middleware.ts` already exists. If yes, merge the empty-roles redirect logic into it (preserving any existing routing logic). If no, create it per 30.1-RESEARCH.md "Code changes section 2".

The middleware:
- Runs `auth()` to get the session
- If no session, returns next() (let the existing public-route handling redirect to login)
- Reads `session.user.roles`
- If roles is empty AND the path isn't in the allowlist (`/pending-approval`, `/login`, `/api`, `/forgot-password`, `/reset-password`, or starts with `/_next`), redirects to `/pending-approval`
- Otherwise next()

The matcher should match all routes except static assets.

**Important:** test that this doesn't break Credentials login. A Credentials user with non-empty roles should pass through unaffected.
  </action>
  <verify>
    `npx tsc --noEmit` clean. Visit `/` as a regular user (Brian, with roles) — works. Manually clear roles on a test user (`UPDATE users SET roles='{}' WHERE email='test@no-bshomes.com'` — but only if such a test user exists; otherwise skip the live test) and confirm they redirect to /pending-approval.
  </verify>
  <done>
    Empty-role users land on /pending-approval automatically.
  </done>
</task>

<task type="auto">
  <name>Task 3: /pending-approval page</name>
  <files>app/src/app/pending-approval/page.tsx</files>
  <action>
Per 30.1-RESEARCH.md "Code changes section 3" — server component, simple layout. Includes:
- Page title: "Pending Approval"
- Body: explains that the user has signed in successfully but needs Owner approval
- Shows the user's email (from session)
- Sign-out button (calls `signOut` server action)
- If the user happens to land here with non-empty roles (race condition), redirect to `/`

Match the existing app's typography + spacing. Centered layout, max-width container, muted text styling.
  </action>
  <verify>
    Visit /pending-approval while logged in — page renders. Sign-out works. If you have non-empty roles, redirects to /.
  </verify>
  <done>
    Users get a clear, calm explanation of why they can't proceed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Login page — add Google sign-in button</name>
  <files>app/src/app/login/page.tsx</files>
  <action>
Add a "Sign in with Google" button at the top of the login form, above the existing email/password fields. Per 30.1-RESEARCH.md "Code changes section 4".

Visual layout:
1. Page title (existing)
2. Big Google button (full-width on mobile, prominent style — see below)
3. Divider with "Or sign in with email" text
4. Existing email/password form (unchanged)
5. Forgot password link (existing)

The Google button should follow Google's branding guidelines: white background, gray border, the Google "G" four-color icon, "Sign in with Google" text. Inline the SVG for the G icon (10-line SVG; don't add a new dependency).

Use a server-action wrapper:
```tsx
<form action={async () => { "use server"; const { signIn } = await import("@/auth"); await signIn("google", { redirectTo: "/" }); }}>
  <button type="submit" className="...">...</button>
</form>
```

(The dynamic import of `signIn` inside the server action is to keep the page a client component if it already is one — adapt to whatever the existing login page uses.)
  </action>
  <verify>
    `npx tsc --noEmit` clean. Visit /login. Google button appears at top. Email/password form still below. Click Google button — bounces to Google's OAuth consent screen (will fail until Brian sets the env vars; that's expected).
  </verify>
  <done>
    Both login paths visible. Google flow waits on env vars; Credentials flow unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: /admin/users — pending badge + sort-first</name>
  <files>app/src/components/admin/users-table.tsx</files>
  <action>
Update the table:
- Sort users by `roles.length === 0 first` (pending users on top), then by created_at desc as the secondary sort.
- Add a small yellow `Pending` badge next to the name when `roles.length === 0`. Use the existing Badge component (variant `secondary` with custom yellow classes, or shadcn-style `bg-yellow-100 text-yellow-800` etc.).
- Optional polish: a small tooltip on the badge explaining "Auto-provisioned via Google sign-in. Assign roles to grant access."

Don't break the existing edit / deactivate / reset-password buttons — they should work the same on pending users.
  </action>
  <verify>
    `npx tsc --noEmit` clean. Visit /admin/users (as Brian). All existing users render normally (no badge — they have roles). Manually set Stacee's roles to `[]` (test-only — undo immediately) and confirm she shows up first with the Pending badge.
  </verify>
  <done>
    Pending users are visually obvious to Owner.
  </done>
</task>

<task type="auto">
  <name>Task 6: Document the env vars</name>
  <files>app/.env.local.example</files>
  <action>
Add `GOOGLE_CLIENT_ID=` and `GOOGLE_CLIENT_SECRET=` placeholder entries to `app/.env.local.example` with a brief comment pointing to `docs/google-services.md` section 3 for setup steps.

If `.env.local.example` doesn't exist, create it from scratch by listing all the env vars that are documented to live in `app/.env.local` (DATABASE_URL, AZURE_STORAGE_CONNECTION_STRING, RESEND_API_KEY, AUTH_SECRET, etc. — populate with empty values + comments). This is the canonical "what env vars do I need" file for future devs.

If it does exist, just append the two new keys.
  </action>
  <verify>
    File exists, has both keys.
  </verify>
  <done>
    Future setup is self-documenting.
  </done>
</task>

</tasks>

<verification>
End-to-end after the plan ships AND Brian sets the env vars:

1. **Unauthenticated visit to `/login`** shows Google button + email/password form.
2. **Click "Sign in with Google" with brian@no-bshomes.com** (existing user) → reuses Brian's row → redirected to dashboard with full owner access.
3. **Click "Sign in with Google" with chris@no-bshomes.com** (new Workspace user) → first time creates a row with roles=[] → redirected to `/pending-approval`. Brian visits `/admin/users`, sees Chris pending at the top, assigns 'sales' role, Chris reloads `/pending-approval` → redirected to `/` with sales access.
4. **Click "Sign in with Google" with foo@gmail.com** → rejected at the Google OAuth consent screen (Internal audience blocks it), or rejected at NextAuth signIn callback (belt-and-suspenders).
5. **Existing email/password login as Stacee** continues to work unchanged.

Until Brian sets `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` in Netlify, the Google button errors out gracefully (NextAuth shows "configuration error" screen). The Credentials flow keeps working — no regression for the current users.

Non-blocking observations:
- The /admin/users page may need a "Pending" filter chip if pending users pile up (defer until you actually see this happen)
- We could add a one-shot "approval email" to Brian when a new user provisions; defer
</verification>
