---
phase: 16-buyers-list-crm
plan: 05
type: execute
wave: 3
depends_on: ["16-01"]
files_modified:
  - app/src/app/(dashboard)/page.tsx
  - app/src/components/buyer-followup-widget.tsx
autonomous: true
requirements:
  - BUYER-08

must_haves:
  truths:
    - "Dashboard shows overdue buyer follow-up reminders"
    - "Each reminder links to the buyer detail page"
    - "Widget only appears when there are overdue follow-ups"
  artifacts:
    - path: "app/src/components/buyer-followup-widget.tsx"
      provides: "Dashboard widget showing overdue buyer follow-ups"
      min_lines: 30
    - path: "app/src/app/(dashboard)/page.tsx"
      provides: "Dashboard page with follow-up widget integrated"
      contains: "BuyerFollowupWidget"
  key_links:
    - from: "app/src/app/(dashboard)/page.tsx"
      to: "app/src/lib/buyer-queries.ts"
      via: "server-side fetch"
      pattern: "getOverdueBuyerFollowups"
    - from: "app/src/components/buyer-followup-widget.tsx"
      to: "/buyers/[id]"
      via: "Link href"
      pattern: 'href.*"/buyers/'
---

<objective>
Add a follow-up reminder widget to the main dashboard showing overdue buyer follow-ups.

Purpose: Ensures buyers don't fall through the cracks between deals. Overdue reminders are visible on every dashboard visit.
Output: Follow-up widget component integrated into dashboard page
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-buyers-list-crm/16-RESEARCH.md
@.planning/phases/16-buyers-list-crm/16-01-SUMMARY.md

@app/src/app/(dashboard)/page.tsx
@app/src/lib/buyer-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Follow-up reminder widget on dashboard</name>
  <files>app/src/components/buyer-followup-widget.tsx, app/src/app/(dashboard)/page.tsx</files>
  <action>
1. Create app/src/components/buyer-followup-widget.tsx ("use client"):
   - Props: `{ buyers: OverdueBuyer[] }`
   - Render only when buyers.length > 0 (return null if empty — widget disappears when all follow-ups are done)
   - Compact card with header: "Overdue Follow-Ups" with Bell icon (lucide-react) and count badge
   - List of overdue buyers: each row shows buyer name (link to /buyers/[id]), follow-up date formatted with date-fns, and "days overdue" count in red text
   - Max 5 shown with "View all {N} overdue" link to /buyers?status=active (or similar filter)
   - Visual style: zinc card background with amber/orange border-left accent for urgency. Compact padding. Matches dashboard aesthetic.

2. Update app/src/app/(dashboard)/page.tsx:
   - Import getOverdueBuyerFollowups from buyer-queries
   - Add to existing Promise.all data fetch (adds ~1ms per RESEARCH.md)
   - Render BuyerFollowupWidget above the property grid (below StatsBar and filters, above DashboardPropertyGrid)
   - Pass overdue buyers as prop
   - Widget naturally disappears when no overdue follow-ups exist
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard shows overdue buyer follow-up widget when reminders exist. Each buyer links to detail page. Widget hidden when no overdue follow-ups. Dashboard performance unaffected.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- Dashboard page imports and renders BuyerFollowupWidget
- Widget shows only when overdue follow-ups exist
- Clicking buyer name navigates to /buyers/[id]
</verification>

<success_criteria>
- Follow-up widget renders on dashboard with overdue buyer reminders
- Buyer names link to /buyers/[id] detail page
- Widget hidden when no overdue follow-ups
- Dashboard load time not noticeably impacted
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-buyers-list-crm/16-05-SUMMARY.md`
</output>
