---
phase: 16-buyers-list-crm
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/src/app/(dashboard)/buyers/page.tsx
  - app/src/components/buyers-list-table.tsx
  - app/src/components/buyer-csv-import.tsx
  - app/src/app/api/buyers/export/route.ts
  - app/src/components/app-sidebar.tsx
  - app/src/components/bottom-nav.tsx
  - app/src/components/command-menu.tsx
autonomous: true
requirements:
  - BUYER-02
  - BUYER-09
  - BUYER-10
  - BUYER-12

must_haves:
  truths:
    - "User can view a searchable, filterable buyers list at /buyers"
    - "User can filter by search text, tag, status, target area, funding type"
    - "User can import buyers from CSV with column mapping"
    - "User can export buyers to CSV"
    - "Sidebar and bottom nav link to /buyers (not /deals/buyers)"
  artifacts:
    - path: "app/src/app/(dashboard)/buyers/page.tsx"
      provides: "Buyers list page with server-side data fetch"
      min_lines: 30
    - path: "app/src/components/buyers-list-table.tsx"
      provides: "Searchable/filterable table component"
      min_lines: 80
    - path: "app/src/components/buyer-csv-import.tsx"
      provides: "CSV upload + column mapping UI"
      min_lines: 60
    - path: "app/src/app/api/buyers/export/route.ts"
      provides: "CSV export route handler"
      exports: ["GET"]
  key_links:
    - from: "app/src/app/(dashboard)/buyers/page.tsx"
      to: "app/src/lib/buyer-queries.ts"
      via: "server-side fetch"
      pattern: "getBuyersForList|getAllBuyerTags"
    - from: "app/src/components/buyer-csv-import.tsx"
      to: "app/src/lib/buyer-actions.ts"
      via: "useTransition direct call"
      pattern: "importBuyers"
    - from: "app/src/components/app-sidebar.tsx"
      to: "/buyers"
      via: "href"
      pattern: 'href.*"/buyers"'
---

<objective>
Create the top-level /buyers list page with searchable/filterable table, CSV import/export, and navigation updates.

Purpose: Replaces the old /deals/buyers sub-page with a first-class CRM page. Users can search, filter, import, and export their buyer list.
Output: /buyers page, table component, CSV import component, export API route, updated navigation
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-buyers-list-crm/16-RESEARCH.md
@.planning/phases/16-buyers-list-crm/16-01-SUMMARY.md

@app/src/components/app-sidebar.tsx
@app/src/components/bottom-nav.tsx
@app/src/components/command-menu.tsx
@app/src/app/(dashboard)/deals/buyers/page.tsx
@app/src/components/buyer-list.tsx
@app/src/components/buyer-intake-form.tsx
@app/src/app/api/export/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /buyers list page with searchable table, filters, and inline add form</name>
  <files>app/src/app/(dashboard)/buyers/page.tsx, app/src/components/buyers-list-table.tsx</files>
  <action>
1. Create app/src/app/(dashboard)/buyers/page.tsx (server component):
   - Fetch data server-side: `getBuyersForList(filters)` and `getAllBuyerTags()` in parallel via Promise.all
   - Read URL searchParams for filters (search, tag, status, targetArea, fundingType) — same pattern as dashboard page
   - Pass data as props to BuyersListTable client component
   - Include page header "Buyers" with count badge, and action buttons for "Add Buyer" and "Import CSV"
   - "Add Buyer" opens the existing BuyerIntakeForm in a Dialog
   - "Import CSV" opens BuyerCsvImport component in a Dialog
   - CSV export: `<a href="/api/buyers/export" download>` button (anchor tag, same Phase 06 pattern)

2. Create app/src/components/buyers-list-table.tsx ("use client"):
   - Props: `{ buyers: BuyerWithTags[]; tags: string[] }`
   - Search input at top (filters client-side by name, phone, email — data set is small)
   - Filter row: tag dropdown (populated from tags prop), status toggle (Active/Inactive/All), target area text input, funding type dropdown (cash/hard_money/both/all)
   - Filters update URL searchParams via router.push (same pattern as dashboard filters)
   - Table rows: name (clickable link to /buyers/[id]), phone (tel: link), email (mailto: link), buy box summary (truncated), tags as small Badge components, status badge (green Active / gray Inactive), last contact date (formatted with date-fns), follow-up date (highlight if overdue in red)
   - Empty state: "No buyers yet. Add your first buyer or import from CSV."
   - Use zinc/violet theme consistent with UI revamp (Phase 11 palette)
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Visiting /buyers shows a searchable, filterable table of all buyers with name links to detail, contact links, tags, status, and dates. Filters update URL params.</done>
</task>

<task type="auto">
  <name>Task 2: CSV import/export and navigation updates</name>
  <files>app/src/components/buyer-csv-import.tsx, app/src/app/api/buyers/export/route.ts, app/src/components/app-sidebar.tsx, app/src/components/bottom-nav.tsx, app/src/components/command-menu.tsx</files>
  <action>
1. Create app/src/components/buyer-csv-import.tsx ("use client"):
   - File upload input (accept=".csv")
   - On file select: FileReader reads file, split by newlines, parse first row as headers
   - Show column mapping UI: for each buyer field (name, phone, email, buyBox, minPrice, maxPrice, fundingType, targetAreas, rehabTolerance, notes), show a dropdown populated with CSV headers. Auto-map columns where header name matches (case-insensitive). "name" is required, rest optional.
   - Preview table showing first 5 rows with mapped columns
   - "Import" button: calls `importBuyers(mappedRows)` via useTransition (direct call, not FormData — per RESEARCH.md Pitfall 5)
   - Show results: "{N} imported, {M} errors" with error details expandable
   - Handle quoted CSV fields: split on comma but respect double-quoted strings (simple regex or manual parser — data is small)

2. Create app/src/app/api/buyers/export/route.ts:
   - Export GET handler
   - Call getBuyersForExport()
   - Build CSV using buildCsv pattern (JSON.stringify per cell): columns Name, Phone, Email, Buy Box, Min Price, Max Price, Funding Type, Target Areas, Rehab Tolerance, Tags, Status, Last Contacted, Follow-Up Date, Notes
   - Return Response with Content-Type text/csv and Content-Disposition attachment filename "buyers-YYYY-MM-DD.csv"

3. Update app/src/components/app-sidebar.tsx:
   - Change Buyers href from "/deals/buyers" to "/buyers"
   - Update active detection: remove the special `/deals/buyers` exclusion from deals active check. Buyers active when `pathname.startsWith("/buyers")`

4. Update app/src/components/bottom-nav.tsx:
   - Replace Campaigns with Buyers in mobile bottom nav (5 slots: Dashboard, Deals, Buyers, Analytics, Map). Campaigns remains accessible from desktop sidebar.
   - Use Users icon for Buyers

5. Update app/src/components/command-menu.tsx:
   - If buyers entry exists, update href to "/buyers". If not, add it.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CSV import parses file, shows column mapping, imports via server action. CSV export downloads buyers-YYYY-MM-DD.csv. Sidebar links to /buyers. Bottom nav shows Buyers instead of Campaigns. Command menu navigates to /buyers.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- /buyers page renders with table, filters, add/import/export buttons
- Sidebar "Buyers" link points to /buyers
- Bottom nav shows Buyers icon in mobile
- /api/buyers/export returns valid CSV
</verification>

<success_criteria>
- /buyers page shows searchable, filterable buyers table
- CSV import with column mapping UI works via useTransition
- CSV export downloads valid CSV file
- All navigation (sidebar, bottom nav, command menu) updated to /buyers
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-buyers-list-crm/16-02-SUMMARY.md`
</output>
