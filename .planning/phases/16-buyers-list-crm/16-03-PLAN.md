---
phase: 16-buyers-list-crm
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/src/app/(dashboard)/buyers/[id]/page.tsx
  - app/src/components/buyer-detail-header.tsx
  - app/src/components/buyer-timeline.tsx
  - app/src/components/buyer-deal-history.tsx
autonomous: true
requirements:
  - BUYER-03
  - BUYER-04
  - BUYER-06
  - BUYER-07
  - BUYER-08

must_haves:
  truths:
    - "User can view a buyer detail page at /buyers/[id] with full profile"
    - "User can see buyer communication timeline with icons per event type"
    - "User can log communication events (call, voicemail, email, text, meeting, note)"
    - "User can see buyer-deal interaction history (blasted, interested, closed)"
    - "User can set/clear follow-up reminder date"
    - "User can add/remove tags on a buyer"
  artifacts:
    - path: "app/src/app/(dashboard)/buyers/[id]/page.tsx"
      provides: "Buyer detail page with server-side data fetch"
      min_lines: 40
    - path: "app/src/components/buyer-detail-header.tsx"
      provides: "Buyer profile header with tags, follow-up, status"
      min_lines: 60
    - path: "app/src/components/buyer-timeline.tsx"
      provides: "Chronological communication timeline"
      min_lines: 80
    - path: "app/src/components/buyer-deal-history.tsx"
      provides: "Deal interaction cards with status badges"
      min_lines: 40
  key_links:
    - from: "app/src/app/(dashboard)/buyers/[id]/page.tsx"
      to: "app/src/lib/buyer-queries.ts"
      via: "server-side fetch"
      pattern: "getBuyerById|getBuyerTimeline|getBuyerDealInteractions"
    - from: "app/src/components/buyer-detail-header.tsx"
      to: "app/src/lib/buyer-actions.ts"
      via: "server actions"
      pattern: "setBuyerFollowUp|addBuyerTag|removeBuyerTag"
    - from: "app/src/components/buyer-timeline.tsx"
      to: "app/src/lib/buyer-actions.ts"
      via: "logBuyerCommEvent"
      pattern: "logBuyerCommEvent"
---

<objective>
Create the buyer detail page at /buyers/[id] with profile header, communication timeline, deal interaction history, tag management, and follow-up reminders.

Purpose: This is the CRM heart — a single view of everything about a buyer. Communication tracking, deal history, and follow-up management all on one page.
Output: Buyer detail page, header component, timeline component, deal history component
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-buyers-list-crm/16-RESEARCH.md
@.planning/phases/16-buyers-list-crm/16-01-SUMMARY.md

@app/src/components/activity-timeline.tsx
@app/src/lib/contact-event-queries.ts
@app/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buyer detail page and profile header with tags + follow-up</name>
  <files>app/src/app/(dashboard)/buyers/[id]/page.tsx, app/src/components/buyer-detail-header.tsx</files>
  <action>
1. Create app/src/app/(dashboard)/buyers/[id]/page.tsx (server component):
   - Extract `id` from params
   - Fetch in parallel: getBuyerById(id), getBuyerTimeline(id), getBuyerDealInteractions(id), getAllBuyerTags()
   - If buyer not found, call notFound()
   - Render: BuyerDetailHeader (profile + tags + follow-up), two-column layout on desktop (single column mobile):
     - Left/main column: BuyerTimeline (communication log + add event form)
     - Right/sidebar column: BuyerDealHistory (deal interaction cards)
   - Back link to /buyers

2. Create app/src/components/buyer-detail-header.tsx ("use client"):
   - Props: `{ buyer: BuyerWithTags; allTags: string[] }`
   - Display: name (large heading), phone (tel: link), email (mailto: link), buy box, price range ($min - $max), funding type, target areas, rehab tolerance, status badge (Active green / Inactive gray)
   - Notes section (read-only display of buyer.notes)
   - Tags row: render existing tags as dismissible Badge components (X button calls removeBuyerTag). "Add tag" input with datalist populated from allTags for autocomplete suggestions. Submit calls addBuyerTag via form action.
   - Follow-up section: if followUpDate set, show date with "Clear" button. If overdue (date < today), show in red with "Overdue" label. "Set Follow-Up" date input calls setBuyerFollowUp via form action. Use native date input.
   - "Edit Buyer" button opens existing BuyerIntakeForm in Dialog (pass buyer prop for edit mode)
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Buyer detail page renders at /buyers/[id] with full profile, tags (add/remove), follow-up date (set/clear/overdue indicator), and edit button</done>
</task>

<task type="auto">
  <name>Task 2: Communication timeline and deal interaction history</name>
  <files>app/src/components/buyer-timeline.tsx, app/src/components/buyer-deal-history.tsx</files>
  <action>
1. Create app/src/components/buyer-timeline.tsx ("use client"):
   - Props: `{ buyerId: string; entries: BuyerTimelineEntry[] }`
   - "Log Event" form at top: event type dropdown (Called, Left Voicemail, Emailed, Sent Text, Met In Person, Note — exclude deal_blast which is auto-logged), notes textarea, submit button. Uses logBuyerCommEvent server action via form action.
   - Timeline list below form: chronological (newest first), each entry shows:
     - Icon per event type: Phone for called_buyer, PhoneOff for left_voicemail, Mail for emailed_buyer, MessageSquare for sent_text, Users for met_in_person, Megaphone for deal_blast, StickyNote for note. Use lucide-react icons.
     - Color coding: calls = blue, emails = green, texts = purple, meetings = orange, deal blasts = yellow, notes = gray
     - Event type label, notes (if any), relative time via date-fns formatDistanceToNow
     - For deal_blast entries: show linked deal address as a clickable link to /deals/[dealId]
     - For deal_interaction entries: show "Deal: {address}" with status badge (blasted=yellow, interested=blue, closed=green)
   - Filter row: optional type filter buttons to show only certain event types
   - Empty state: "No communication history yet. Log your first interaction above."

2. Create app/src/components/buyer-deal-history.tsx ("use client"):
   - Props: `{ interactions: BuyerDealInteraction[] }`
   - Card list showing each deal the buyer has interacted with:
     - Deal address (link to /deals/[dealId])
     - Deal city
     - Interaction status badge: blasted (yellow), interested (blue), closed (green)
     - Dates: created, last updated
   - Section header: "Deal History ({count})"
   - Empty state: "No deal interactions yet."
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Timeline shows all buyer communication events with icons, colors, and deal links. Event logging form works. Deal history shows interaction cards with status badges. Both components render correctly on mobile.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- /buyers/[id] renders buyer profile, tags, follow-up, timeline, deal history
- Log event form submits and new entry appears
- Tags can be added and removed
- Follow-up date can be set and cleared
</verification>

<success_criteria>
- Buyer detail page at /buyers/[id] with full profile header
- Communication timeline with icons/colors per event type and log form
- Deal interaction history with status badges
- Tag management (add with autocomplete, remove with X)
- Follow-up date management with overdue highlighting
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-buyers-list-crm/16-03-SUMMARY.md`
</output>
