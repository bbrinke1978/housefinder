---
phase: 16-buyers-list-crm
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/src/app/(dashboard)/deals/[id]/page.tsx
  - app/src/components/deal-blast-generator.tsx
  - app/src/components/buyer-list.tsx
  - app/src/lib/deal-queries.ts
autonomous: true
requirements:
  - BUYER-05
  - BUYER-06
  - BUYER-11

must_haves:
  truths:
    - "Deal detail page shows auto-matched buyers by price AND target area"
    - "Matched buyers are pre-selected in deal blast flow"
    - "Deal blast can be emailed to selected buyers via Resend"
    - "Deal blast auto-logs to buyer communication history"
    - "Buyer-deal interaction status can be updated (blasted/interested/closed)"
  artifacts:
    - path: "app/src/lib/deal-queries.ts"
      provides: "Enhanced getMatchingBuyers with area matching"
      contains: "getMatchingBuyersForDeal"
    - path: "app/src/components/deal-blast-generator.tsx"
      provides: "Email blast with auto-logging"
      contains: "logDealBlast"
    - path: "app/src/components/buyer-list.tsx"
      provides: "Updated buyer list with match indicators and interaction status"
      contains: "BuyerWithMatchInfo"
  key_links:
    - from: "app/src/components/deal-blast-generator.tsx"
      to: "app/src/lib/buyer-actions.ts"
      via: "logDealBlast server action"
      pattern: "logDealBlast"
    - from: "app/src/app/(dashboard)/deals/[id]/page.tsx"
      to: "app/src/lib/deal-queries.ts"
      via: "getMatchingBuyersForDeal"
      pattern: "getMatchingBuyersForDeal"
---

<objective>
Integrate buyer CRM into the deal detail page: enhanced buyer matching (price + area), email blast with auto-logging, and buyer-deal interaction tracking.

Purpose: Closes the loop between deals and buyers — matching, blasting, and tracking form a unified workflow on the deal detail page.
Output: Enhanced deal detail with smart matching, email blast capability, and interaction tracking
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-buyers-list-crm/16-RESEARCH.md
@.planning/phases/16-buyers-list-crm/16-01-SUMMARY.md

@app/src/app/(dashboard)/deals/[id]/page.tsx
@app/src/components/deal-blast-generator.tsx
@app/src/components/buyer-list.tsx
@app/src/lib/deal-queries.ts
@app/src/lib/buyer-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced buyer matching and deal detail integration</name>
  <files>app/src/lib/deal-queries.ts, app/src/app/(dashboard)/deals/[id]/page.tsx, app/src/components/buyer-list.tsx</files>
  <action>
1. In deal-queries.ts, replace or add `getMatchingBuyersForDeal(dealPrice: number, dealCity: string)`:
   - Query active buyers where price range matches (existing logic: null min/max treated as open)
   - Post-query: compute `matchesArea` — true if buyer.targetAreas is null OR buyer.targetAreas.toLowerCase() includes dealCity.toLowerCase()
   - Compute `isFullMatch` — price match AND area match
   - Left join buyer_tags to include tags array per buyer
   - Return BuyerWithMatchInfo[] sorted: full matches first, then price-only matches, then rest
   - Keep existing `getMatchingBuyers` function for backward compatibility (or rename if no other callers)

2. In deals/[id]/page.tsx:
   - Replace the existing getMatchingBuyers call with getMatchingBuyersForDeal(deal.offerPrice ?? deal.mao ?? 0, deal.city)
   - Pass the enhanced buyer list to BuyerList and DealBlastGenerator components
   - Fetch buyer-deal interactions for this deal to show current status per buyer

3. In buyer-list.tsx:
   - Update to accept BuyerWithMatchInfo[] instead of plain Buyer[]
   - Show match indicator per buyer: green checkmark badge "Full Match" (price + area), yellow badge "Price Match" (price only), no badge (manual add)
   - Add "Interested" / "Closed" toggle buttons per buyer row that call updateBuyerDealInteraction server action
   - Show current interaction status badge if buyer has been blasted/interested/closed for this deal
   - Buyer name links to /buyers/[id] detail page
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Deal detail shows auto-matched buyers sorted by match quality (full match > price only). Match badges visible. Interaction status toggleable per buyer.</done>
</task>

<task type="auto">
  <name>Task 2: Email blast with auto-logging to buyer CRM</name>
  <files>app/src/components/deal-blast-generator.tsx</files>
  <action>
1. Enhance DealBlastGenerator to support email blasting:
   - Keep existing copy-to-clipboard functionality
   - Add "Email Blast" button below "Copy to Clipboard" — only enabled when deal status is in BLAST_STATUSES (under_contract, marketing, assigned, closing)
   - Email blast flow:
     a. Show list of matched buyers with checkboxes (pre-check "Full Match" buyers, uncheck others). Include buyer name + email. Buyers without email are shown disabled with "No email" text.
     b. "Send Blast" button: for each checked buyer with email, call a new server action `sendDealBlast(dealId, buyerId, buyerEmail, blastText)` that:
        - Sends email via Resend (import from existing mail config in scraperConfig — use same RESEND_API_KEY pattern from Phase 12)
        - Calls logDealBlast(buyerId, dealId) to auto-log the communication event and upsert interaction
     c. Show progress: "{sent}/{total} emails sent"
     d. On complete: show success toast with count

2. Create the sendDealBlast server action in buyer-actions.ts:
   - Accept: dealId, buyerId, buyerEmail, blastText (the formatted blast string)
   - Fetch mail settings from scraperConfig (mail.resendApiKey, mail.fromEmail, mail.fromName)
   - Send email via Resend with subject "Deal Available - {dealAddress}" and plain text body = blastText
   - Call logDealBlast(buyerId, dealId)
   - Return { success: true } or { error: string }

Note: If Resend API key is not configured, show a message linking to /settings/mail to configure it. Don't block the entire blast UI — copy-to-clipboard still works without email.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Deal blast generator has email send capability alongside copy-to-clipboard. Matched buyers shown with checkboxes. Email sends via Resend. Auto-logs to buyer communication history and upserts buyer-deal interaction.</done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- Deal detail shows enhanced buyer matching (price + area)
- Deal blast generator has email blast button with buyer selection
- Blasting a deal auto-logs to buyer timeline and creates buyer-deal interaction
- Buyer names link to /buyers/[id]
</verification>

<success_criteria>
- getMatchingBuyersForDeal returns buyers sorted by match quality with area matching
- Buyer list on deal detail shows match badges and interaction status
- Email blast sends to selected buyers via Resend with auto-logging
- Copy-to-clipboard still works as fallback without email config
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-buyers-list-crm/16-04-SUMMARY.md`
</output>
