---
phase: 14-mobile-photo-capture
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/src/components/photo-upload.tsx
  - app/src/components/photo-gallery.tsx
  - app/src/components/photo-tab.tsx
  - app/src/app/(dashboard)/deals/[id]/page.tsx
  - app/package.json
autonomous: true
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03
  - PHOTO-09

must_haves:
  truths:
    - "User can select multiple photos or take a single photo and upload with category tag"
    - "User can see per-file upload progress (pending/uploading/done/error)"
    - "Deal detail page has a Photos tab showing all photos grouped by category"
    - "Clicking a photo opens a full-screen lightbox with swipe and caption overlay"
    - "User can set cover photo, delete photos, and edit captions from the gallery"
    - "iOS Safari users can both take photos (camera) and select from library (gallery)"
  artifacts:
    - path: "app/src/components/photo-upload.tsx"
      provides: "Multi-photo upload with category picker, dual input for iOS, per-file progress"
      min_lines: 80
    - path: "app/src/components/photo-gallery.tsx"
      provides: "Photo grid grouped by category with YARL lightbox integration"
      min_lines: 60
    - path: "app/src/components/photo-tab.tsx"
      provides: "Photos tab wrapper for deal detail page, composes upload + gallery"
      min_lines: 30
    - path: "app/src/app/(dashboard)/deals/[id]/page.tsx"
      provides: "Photos tab added to deal detail tabbed layout"
      contains: "Photos"
  key_links:
    - from: "app/src/components/photo-upload.tsx"
      to: "app/src/lib/photo-actions.ts"
      via: "uploadPhoto server action call per file"
      pattern: "uploadPhoto"
    - from: "app/src/components/photo-gallery.tsx"
      to: "yet-another-react-lightbox"
      via: "dynamic import with ssr:false"
      pattern: "dynamic.*import.*yet-another-react-lightbox"
    - from: "app/src/app/(dashboard)/deals/[id]/page.tsx"
      to: "app/src/components/photo-tab.tsx"
      via: "PhotoTab rendered inside TabsContent"
      pattern: "PhotoTab"
---

<objective>
Build the photo upload component and deal detail Photos tab with gallery lightbox.

Purpose: Enables the primary "deal-first" photo workflow — navigate to a deal, upload photos with category tags, browse them in a grouped gallery with full-screen lightbox.
Output: PhotoUpload component, PhotoGallery with YARL lightbox, PhotoTab on deal detail page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-mobile-photo-capture/14-RESEARCH.md
@.planning/phases/14-mobile-photo-capture/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs): -->
From app/src/lib/photo-actions.ts:
```typescript
"use server";
export async function uploadPhoto(formData: FormData): Promise<void>;
export async function setPhotoCover(photoId: string, dealId: string): Promise<void>;
export async function deletePhoto(photoId: string): Promise<void>;
export async function updatePhotoCaption(photoId: string, caption: string): Promise<void>;
```

From app/src/lib/photo-queries.ts:
```typescript
export type PhotoWithSasUrl = PropertyPhotoRow & { sasUrl: string };
export async function getDealPhotos(dealId: string): Promise<PhotoWithSasUrl[]>;
export async function getDealCoverPhoto(dealId: string): Promise<{ blobName: string; sasUrl: string } | null>;
```

From app/src/db/schema.ts:
```typescript
export const photoCategory = pgEnum("photo_category", ["exterior","kitchen","bathroom","living","bedroom","garage","roof","foundation","yard","other"]);
export type PropertyPhotoRow = InferSelectModel<typeof propertyPhotos>;
```

<!-- Existing deal detail page tabs (currently 4 tabs): -->
From app/src/app/(dashboard)/deals/[id]/page.tsx:
- Tabs: Overview, Analysis, Financials, Activity
- Uses: Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
- Data fetched at page level, passed as props to tab components (pattern from BudgetTab, ContractTab)

<!-- Existing resizeImage pattern from receipt-upload.tsx: -->
From app/src/components/receipt-upload.tsx:
```typescript
async function resizeImage(file: File): Promise<Blob> {
  // Canvas resize to max 1920px longest side, JPEG 0.8 quality
  // Uses URL.createObjectURL + Image + Canvas + toBlob
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install YARL and create photo upload + gallery components</name>
  <files>app/package.json, app/src/components/photo-upload.tsx, app/src/components/photo-gallery.tsx</files>
  <action>
1. Install `yet-another-react-lightbox` in the app directory: `cd app && npm install yet-another-react-lightbox`.

2. Create `app/src/components/photo-upload.tsx` as "use client" component:
   - Props: `{ dealId?: string; propertyId?: string; onUploadComplete?: () => void }`.
   - Internal state: `queue: PhotoUploadState[]` where each item has `{ id: string, file: File, status: "pending"|"uploading"|"done"|"error", previewUrl: string }`, `category: string` (default "other"), `showCategoryPicker: boolean`.
   - Copy the `resizeImage` function from `receipt-upload.tsx` (canvas resize to max 1600px per CONTEXT.md decision, JPEG 0.8 quality). Change max from 1920 to 1600.
   - TWO file inputs for iOS Safari compatibility (per RESEARCH.md Pitfall 5):
     a) "Take Photo" button with `<input type="file" accept="image/*" capture="environment">` (single shot, no `multiple`)
     b) "Add from Library" button with `<input type="file" accept="image/*" multiple>` (no `capture`)
   - Both inputs share the same `handleCapture` which adds files to the queue with status "pending" and creates preview URLs via `URL.createObjectURL`.
   - After files are queued, show a category selector (dropdown or radio group) with the 10 predefined categories: Exterior, Kitchen, Bathroom, Living, Bedroom, Garage, Roof, Foundation, Yard, Other.
   - Optional caption textarea per photo (collapsible, not required).
   - "Upload N Photos" button triggers sequential upload: for each item, set status to "uploading", call `resizeImage(file)`, create FormData with file/dealId/propertyId/category/caption, call `uploadPhoto(formData)` server action, set status to "done" or "error".
   - Show upload progress as status badges per file: spinner for uploading, checkmark for done, X for error.
   - After all uploads complete, clear queue, revoke all object URLs, call `onUploadComplete?.()`.
   - Use lucide-react icons: Camera, ImagePlus, Check, X, Upload, Loader2.
   - Style with Tailwind: responsive, dark-mode compatible, matching existing zinc/violet palette.

3. Create `app/src/components/photo-gallery.tsx` as "use client" component:
   - Props: `{ photos: PhotoWithSasUrl[]; dealId?: string; canManage?: boolean }` where canManage enables delete/cover/caption controls.
   - Import YARL lightbox via `next/dynamic` with `ssr: false`. Import CSS: `yet-another-react-lightbox/styles.css` and `yet-another-react-lightbox/plugins/captions.css`.
   - Dynamic import the Captions plugin: `dynamic(() => import("yet-another-react-lightbox/plugins/captions").then(m => m.Captions), { ssr: false })`.
   - Group photos by category. For each category group, render a section header (capitalized category name) and a responsive grid (2 cols on mobile, 3 on sm, 4 on md).
   - Each photo card: aspect-square with `object-cover`, rounded corners. If `isCover`, show a small "Cover" badge. Click opens lightbox at that index.
   - Build slides array from all photos: `{ src: photo.sasUrl, title: category, description: caption }`.
   - Lightbox: open state controlled by `index >= 0`, close sets index to -1. Plugins: [Captions].
   - If canManage, each photo card shows overlay controls on hover/tap:
     a) Star icon to set as cover (calls `setPhotoCover(photo.id, dealId)`)
     b) Trash icon to delete (calls `deletePhoto(photo.id)` with confirm dialog)
     c) Caption edit (inline text input, calls `updatePhotoCaption(photo.id, newCaption)`)
   - Empty state: "No photos yet. Add photos to get started." with camera icon.
  </action>
  <verify>cd app && npx tsc --noEmit 2>&1 | grep -c "error" (should be 0 or only pre-existing errors); grep -c "yet-another-react-lightbox" package.json (should be 1)</verify>
  <done>yet-another-react-lightbox installed. photo-upload.tsx handles dual-input iOS-safe capture with batch upload, per-file progress, and category picker. photo-gallery.tsx renders grouped grid with YARL lightbox, cover/delete/caption management controls.</done>
</task>

<task type="auto">
  <name>Task 2: Photos tab on deal detail page</name>
  <files>app/src/components/photo-tab.tsx, app/src/app/(dashboard)/deals/[id]/page.tsx</files>
  <action>
1. Create `app/src/components/photo-tab.tsx` as "use client" component:
   - Props: `{ photos: PhotoWithSasUrl[]; dealId: string }`.
   - Renders `<PhotoUpload dealId={dealId} />` at the top.
   - Renders `<PhotoGallery photos={photos} dealId={dealId} canManage={true} />` below.
   - Simple composition wrapper — no additional logic.

2. Modify `app/src/app/(dashboard)/deals/[id]/page.tsx`:
   - Add import: `import { PhotoTab } from '@/components/photo-tab';`
   - Add import: `import { getDealPhotos } from '@/lib/photo-queries';`
   - In the parallel data fetch block, add `getDealPhotos(id)` to the Promise.all.
   - Add a 5th tab: "Photos" — insert a new TabsTrigger after "Financials" and before "Activity" with value "photos". Show photo count badge if photos.length > 0 (same badge pattern as contractCount).
   - Add TabsContent for "photos" rendering `<PhotoTab photos={photos} dealId={id} />`.
   - Update the tabMap if needed to handle any legacy tab names.
   - The page already has `export const dynamic = 'force-dynamic'` so SAS URLs will be fresh on each request.
  </action>
  <verify>cd app && npx tsc --noEmit 2>&1 | grep "error" | head -5; grep -c "PhotoTab\|Photos" src/app/\(dashboard\)/deals/\[id\]/page.tsx</verify>
  <done>Deal detail page now has 5 tabs: Overview, Analysis, Financials, Photos, Activity. Photos tab shows upload controls and photo gallery with lightbox. Photo count badge visible when photos exist.</done>
</task>

</tasks>

<verification>
- `npm install` succeeds with yet-another-react-lightbox added to package.json
- TypeScript compilation passes (no new errors)
- Deal detail page renders 5 tabs including Photos
- PhotoUpload component has dual file inputs (camera + gallery) for iOS compatibility
- PhotoGallery uses dynamic import for YARL with ssr: false
</verification>

<success_criteria>
- yet-another-react-lightbox installed and importable
- photo-upload.tsx: dual input (camera/gallery), batch queue, category picker, resizeImage at 1600px, per-file status
- photo-gallery.tsx: grouped grid, YARL lightbox with Captions plugin, cover/delete/caption controls
- photo-tab.tsx: composes upload + gallery for deal detail
- Deal detail page has Photos tab (5th tab) with photo count badge
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-photo-capture/14-02-SUMMARY.md`
</output>
