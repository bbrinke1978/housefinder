---
phase: 02-core-application
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - app/src/app/(dashboard)/settings/page.tsx
  - app/src/components/settings-form.tsx
  - app/src/lib/actions.ts
  - .github/workflows/deploy-app.yml
autonomous: false
requirements:
  - DASH-06

must_haves:
  truths:
    - "User can configure which cities/counties are in their target scope"
    - "App builds and deploys to Azure App Service via GitHub Actions"
  artifacts:
    - path: "app/src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with target city config and theme toggle"
    - path: "app/src/components/settings-form.tsx"
      provides: "Form to manage target cities via scraperConfig table"
    - path: ".github/workflows/deploy-app.yml"
      provides: "GitHub Actions workflow for Azure App Service deployment"
  key_links:
    - from: "app/src/components/settings-form.tsx"
      to: "app/src/db/schema.ts"
      via: "scraperConfig table read/write"
      pattern: "scraperConfig"
    - from: ".github/workflows/deploy-app.yml"
      to: "app/next.config.ts"
      via: "standalone output build"
      pattern: "output.*standalone"
---

<objective>
Build the settings page (target city configuration), create the GitHub Actions deployment workflow, and verify the complete application end-to-end.

Purpose: This is the final plan for Phase 2. The settings page completes DASH-06. The deployment workflow enables CI/CD to Azure App Service. The checkpoint verifies the full application works against real data.

Output: Settings page at /settings with target city management and theme toggle. GitHub Actions workflow for Azure App Service deployment with standalone output. End-to-end verification by the user.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-application/02-CONTEXT.md
@.planning/phases/02-core-application/02-RESEARCH.md
@.planning/phases/02-core-application/02-01-SUMMARY.md
@.planning/phases/02-core-application/02-02-SUMMARY.md
@.planning/phases/02-core-application/02-03-SUMMARY.md
@.planning/phases/02-core-application/02-04-SUMMARY.md

<interfaces>
From app/src/db/schema.ts:
```typescript
export const scraperConfig = pgTable("scraper_config", {
  id: serial("id").primaryKey(),
  key: text("key").notNull().unique(),
  value: text("value").notNull(),
  description: text("description"),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
})
```

From app/src/lib/actions.ts (from prior plans):
```typescript
export async function updateLeadStatus(leadId: string, status: string, note?: string): Promise<void>
export async function addLeadNote(leadId: string, noteText: string): Promise<void>
export async function markLeadViewed(propertyId: string): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build settings page with target city config and deployment workflow</name>
  <files>
    app/src/app/(dashboard)/settings/page.tsx
    app/src/components/settings-form.tsx
    app/src/lib/actions.ts
    .github/workflows/deploy-app.yml
  </files>
  <action>
    1. Add to `app/src/lib/actions.ts`:

       **getTargetCities()** — reads from scraperConfig where key = "target_cities":
       - Returns parsed JSON array of city strings, or default ["Price"] if not set
       - This is a query, not an action, but put it in actions.ts for simplicity (or queries.ts if that file exists)

       **updateTargetCities(cities: string[])** — server action:
       - Call auth() to verify session
       - Validate with zod: cities is array of non-empty strings
       - Upsert scraperConfig: if key "target_cities" exists, update value; otherwise insert
       - Value stored as JSON string: JSON.stringify(cities)
       - revalidatePath("/settings")

    2. Create `app/src/components/settings-form.tsx` — "use client" component:
       - Target Cities section:
         - Display current cities as removable tags/badges
         - Text input + "Add" button to add a new city
         - "Remove" button (X) on each tag to remove a city
         - Save button that calls updateTargetCities server action
         - Show confirmation toast/message on save (use a simple state-based message, no toast library needed)
       - Theme section:
         - Import ThemeToggle component or embed theme selection directly
         - Three options: Light, Dark, System
       - Use shadcn Card for section grouping

    3. Create `app/src/app/(dashboard)/settings/page.tsx` — async Server Component:
       - Fetch current target cities via getTargetCities()
       - Render SettingsForm with cities as initial prop
       - Page title: "Settings"

    4. Create `.github/workflows/deploy-app.yml`:
       - Trigger: push to main on paths ["app/**"], plus workflow_dispatch
       - Jobs: build-and-deploy on ubuntu-latest
       - Steps:
         - Checkout
         - Setup Node 20.x with npm cache (cache-dependency-path: app/package-lock.json)
         - npm ci in app/ directory
         - npm run build in app/ directory
         - Copy static assets: `cp -r .next/static .next/standalone/.next/static` and `cp -r public .next/standalone/public` (in app/ working directory)
         - Deploy using azure/webapps-deploy@v3 with app-name and publish-profile from secrets, package: app/.next/standalone
       - Required GitHub secrets: AZURE_WEBAPP_NAME, AZURE_WEBAPP_PUBLISH_PROFILE
       - Required Azure App Service config: startup command = `node server.js`
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Settings page renders with target city management and theme toggle. GitHub Actions workflow file exists and is valid YAML. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of complete Phase 2 application</name>
  <files>n/a</files>
  <action>
    User verifies the complete Phase 2 application works end-to-end against real Azure PostgreSQL data. All features from Plans 01-05 are tested together.
  </action>
  <verify>User confirms all 15 verification steps pass</verify>
  <done>User types "approved" confirming the application works correctly</done>
  <what-built>Complete Phase 2 application: authenticated dashboard with property list, filters, sorting, new-lead badges, tabbed property detail, lead pipeline with kanban + list views, voice notes, settings page, and deployment workflow.</what-built>
  <how-to-verify>
    1. Start the app locally: `cd app && npm run dev`
    2. Visit http://localhost:3000 — should redirect to /login
    3. Log in with the credentials configured in .env.local
    4. Dashboard: verify stats bar shows counts, property cards display with data from Azure PostgreSQL
    5. Filters: try filtering by city, hot leads, sorting by score vs date
    6. Click a property card to see the detail page with 4 tabs
    7. Overview tab: verify property data displays correctly
    8. Signals tab: verify distress signals show with dates
    9. Notes tab: add a note and verify it appears
    10. Navigate to Pipeline: verify kanban board shows columns
    11. Drag a lead between columns and verify status updates
    12. Toggle to list view and verify it shows the same leads
    13. Navigate to Settings: verify target cities and theme toggle work
    14. Toggle dark mode and verify it applies across the app
    15. Check mobile view (browser dev tools responsive mode): verify bottom nav appears, sidebar hides
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd app && npm run build` completes without errors
2. Settings page reads/writes target cities from scraperConfig table
3. Theme toggle switches and persists across page reload
4. GitHub Actions workflow is valid YAML
5. Full application flow works: login -> dashboard -> property detail -> pipeline -> settings
</verification>

<success_criteria>
- Settings page allows adding/removing target cities
- Theme toggle persists between light/dark/system
- GitHub Actions deployment workflow created for Azure App Service
- User has verified complete application works end-to-end with real data
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-application/02-05-SUMMARY.md`
</output>
