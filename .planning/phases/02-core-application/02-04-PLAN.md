---
phase: 02-core-application
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/app/(dashboard)/pipeline/page.tsx
  - app/src/components/lead-kanban.tsx
  - app/src/components/lead-list.tsx
  - app/src/components/lead-card.tsx
  - app/src/components/voice-note-input.tsx
  - app/src/lib/queries.ts
  - app/src/lib/actions.ts
autonomous: true
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04

must_haves:
  truths:
    - "User can set lead status (New, Contacted, Follow-Up, Closed, Dead)"
    - "User can add timestamped notes to any lead with voice-to-text option"
    - "User can view full pipeline by status in both kanban and list views"
    - "Leads with no contact info are flagged as 'manual skip trace needed'"
  artifacts:
    - path: "app/src/app/(dashboard)/pipeline/page.tsx"
      provides: "Pipeline page with kanban/list toggle"
      min_lines: 30
    - path: "app/src/components/lead-kanban.tsx"
      provides: "Kanban board with drag-and-drop via @hello-pangea/dnd"
    - path: "app/src/components/lead-list.tsx"
      provides: "List view of pipeline filtered by status"
    - path: "app/src/components/voice-note-input.tsx"
      provides: "Web Speech API voice-to-text input"
    - path: "app/src/lib/actions.ts"
      provides: "updateLeadStatus server action"
      exports: ["updateLeadStatus"]
  key_links:
    - from: "app/src/components/lead-kanban.tsx"
      to: "app/src/lib/actions.ts"
      via: "updateLeadStatus on drag end"
      pattern: "updateLeadStatus"
    - from: "app/src/components/lead-kanban.tsx"
      to: "@hello-pangea/dnd"
      via: "DragDropContext, Droppable, Draggable"
      pattern: "import.*DragDropContext.*hello-pangea"
    - from: "app/src/components/voice-note-input.tsx"
      to: "Web Speech API"
      via: "window.SpeechRecognition"
      pattern: "SpeechRecognition"
---

<objective>
Build the lead pipeline page with kanban board, list view toggle, status management, voice-to-text notes, and skip trace flag.

Purpose: This is the investor's CRM view. They need to manage their pipeline of leads through statuses, add notes (including via voice while driving), and quickly identify which leads still need contact information.

Output: Pipeline page at /pipeline with kanban board (drag-and-drop between columns) + list view (filterable by status), status update server actions, voice-to-text note input via Web Speech API, and "manual skip trace needed" flag on leads without contact info.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-application/02-CONTEXT.md
@.planning/phases/02-core-application/02-RESEARCH.md
@.planning/phases/02-core-application/02-01-SUMMARY.md

<interfaces>
From app/src/types/index.ts:
```typescript
export type LeadStatus = "new" | "contacted" | "follow_up" | "closed" | "dead"
export interface PropertyWithLead {
  id: string; address: string; city: string; ownerName: string | null;
  distressScore: number; isHot: boolean; leadStatus: LeadStatus;
  firstSeenAt: Date | null; lastContactedAt: Date | null;
}
export interface LeadNote { ... }
```

From app/src/db/schema.ts:
- leads table: id, propertyId, status, distressScore, isHot, lastContactedAt
- leadNotes table: id, leadId, noteText, noteType, previousStatus, newStatus, createdAt

From app/src/lib/actions.ts (may exist from Plan 03):
```typescript
export async function addLeadNote(leadId: string, noteText: string): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline queries, updateLeadStatus action, and voice input component</name>
  <files>
    app/src/lib/queries.ts
    app/src/lib/actions.ts
    app/src/components/voice-note-input.tsx
  </files>
  <action>
    NOTE: queries.ts and actions.ts likely exist from Plan 02 or Plan 03. APPEND new functions — do not overwrite existing code.

    1. Add to `app/src/lib/queries.ts`:

       **getPipelineLeads()** — returns all leads with property data for the pipeline:
       - Inner join properties + leads on leads.propertyId = properties.id
       - Select: lead id, property id, address, city, ownerName, distressScore, isHot, status (as leadStatus), lastContactedAt, firstSeenAt
       - Order by distressScore desc within each status group
       - No limit (pipeline shows all leads)

    2. Add to `app/src/lib/actions.ts`:

       **updateLeadStatus(leadId: string, status: string, note?: string)** — server action:
       - Call auth() to verify session
       - Validate with zod: leadId is uuid, status is one of the 5 valid values
       - Fetch existing lead status for the status change note
       - Update leads: set status, set lastContactedAt = new Date() when status changes to "contacted"
       - Auto-log status change: insert into leadNotes with noteType = "status_change", recording previousStatus and newStatus
       - If optional note text provided, insert a separate user note as well
       - revalidatePath("/pipeline")
       - Status change note is OPTIONAL (per Claude's discretion from CONTEXT.md — making it optional is the better UX for quick pipeline management)

    3. Create `app/src/components/voice-note-input.tsx` — "use client" component:
       - Microphone button that triggers Web Speech API for voice-to-text dictation
       - Check browser support: `"webkitSpeechRecognition" in window || "SpeechRecognition" in window`
       - If not supported: render button as disabled with title "Voice input requires Chrome"
       - On click: create SpeechRecognition instance, continuous=false, interimResults=false, lang="en-US"
       - Start recognition, set isListening=true
       - On result: call onTranscript callback with the transcript text
       - On end: set isListening=false
       - Visual states: idle (muted icon), listening (red pulsing icon with animate-pulse)
       - Props: `onTranscript: (text: string) => void`
       - The parent component (notes form) appends the transcript to the textarea value
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>getPipelineLeads query compiles. updateLeadStatus server action compiles. VoiceNoteInput component compiles. TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build pipeline page with kanban board, list view toggle, and lead cards</name>
  <files>
    app/src/app/(dashboard)/pipeline/page.tsx
    app/src/components/lead-kanban.tsx
    app/src/components/lead-list.tsx
    app/src/components/lead-card.tsx
  </files>
  <action>
    1. Create `app/src/components/lead-card.tsx` — shared card used in both kanban and list views:
       - Shows: address (bold), city, owner name, distress score badge, hot lead badge
       - Shows: last contacted date formatted with date-fns (e.g., "3 days ago" using formatDistanceToNow)
       - Shows: "Skip trace needed" badge/flag — for Phase 2, ALL leads show this since contact lookup is Phase 3. Use a small orange badge.
       - Card links to /properties/[propertyId] for detail view
       - Compact layout suitable for kanban column width (~250px)

    2. Create `app/src/components/lead-kanban.tsx` — "use client" component:
       - Import DragDropContext, Droppable, Draggable from @hello-pangea/dnd
       - 5 columns: New, Contacted, Follow-Up, Closed, Dead
       - Column labels map: { new: "New", contacted: "Contacted", follow_up: "Follow-Up", closed: "Closed", dead: "Dead" }
       - Each column is a Droppable with droppableId = status value
       - Each lead card is a Draggable with draggableId = lead.id
       - On drag end: optimistic state update (move card in local state), then call updateLeadStatus server action
       - Column header shows status name + count of leads in that column
       - Horizontal scroll on mobile (overflow-x-auto) with min-width columns
       - Column styling: rounded-lg, subtle background color per status

    3. Create `app/src/components/lead-list.tsx` — "use client" component:
       - Alternative view to kanban — shows leads in a flat list
       - Status filter tabs at top (All, New, Contacted, Follow-Up, Closed, Dead)
       - Each lead is a LeadCard with a status dropdown for quick status change
       - On status change from dropdown: call updateLeadStatus server action
       - Sort by distress score (default), with last contacted date visible
       - Include a quick "Add Note" button per lead that opens an inline form with VoiceNoteInput

    4. Create `app/src/app/(dashboard)/pipeline/page.tsx` — async Server Component:
       - Fetch pipeline leads via getPipelineLeads()
       - Render a toggle at the top: Kanban view / List view (use URL searchParam `view=kanban|list`, default kanban)
       - Toggle is a "use client" component or simple Link-based tab pair
       - Pass leads to either LeadKanban or LeadList based on current view
       - Page title: "Pipeline"
       - Empty state: "No leads in pipeline yet" with link to dashboard
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Pipeline page renders with kanban/list toggle. Kanban shows 5 columns with drag-and-drop. List shows filterable lead list with status dropdowns. Lead cards show address, score, last contacted, skip trace flag. Voice input button renders on note forms. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd app && npm run build` completes without errors
2. /pipeline renders kanban view with 5 status columns
3. Dragging a card between columns updates lead status in database
4. Toggle switches between kanban and list views
5. List view filters by status tab
6. Voice note button renders (functional in Chrome only)
7. All leads show "skip trace needed" flag
8. Last contacted date displays on lead cards
</verification>

<success_criteria>
- Pipeline page shows kanban board with 5 columns (New, Contacted, Follow-Up, Closed, Dead)
- Drag-and-drop moves leads between columns and persists status to database
- List view shows same leads filterable by status
- Status changes auto-log as notes with previous/new status
- Voice-to-text input works in Chrome for adding notes hands-free
- "Manual skip trace needed" flag visible on all leads (Phase 3 adds contact lookup)
- Last contacted date visible for follow-up prioritization
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-application/02-04-SUMMARY.md`
</output>
