---
phase: 17-netlify-migration-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/layout.tsx
  - app/src/app/globals.css
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05

must_haves:
  truths:
    - "Playfair Display renders on all headings and Source Sans 3 on all body text"
    - "Warm cream background (#fdfbf7) is visible as default app background"
    - "Brand blue (#1e4d8c) is the primary color for links and CTAs"
    - "Sand accent (#c4884f) is visible on primary action buttons"
    - "Subtle grain texture overlay is visible on background surfaces"
    - "Light mode is the default theme; dark mode shows warm navy palette"
  artifacts:
    - path: "app/src/app/layout.tsx"
      provides: "Font loading via next/font/google"
      contains: "Playfair_Display"
    - path: "app/src/app/globals.css"
      provides: "Warm brand tokens replacing zinc/violet"
      contains: "#1e4d8c"
  key_links:
    - from: "app/src/app/layout.tsx"
      to: "app/src/app/globals.css"
      via: "CSS variables --font-display and --font-body"
      pattern: "--font-display|--font-body"
---

<objective>
Design system foundation: fonts, color tokens, grain overlay, and theme default

Purpose: Replace the zinc/violet design system with the No BS Homes warm brand palette (Playfair Display + Source Sans 3, brand blue/sand/cream) as the single source of truth in globals.css and layout.tsx. All downstream components inherit via semantic CSS tokens.
Output: Updated layout.tsx with new fonts, completely rewritten globals.css with warm brand tokens
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-netlify-migration-design-system/17-CONTEXT.md
@.planning/phases/17-netlify-migration-design-system/17-RESEARCH.md
@app/src/app/layout.tsx
@app/src/app/globals.css
</context>

<interfaces>
<!-- Current layout.tsx font pattern to replace -->
From app/src/app/layout.tsx:
```typescript
import { Inter, Geist_Mono } from "next/font/google";
const inter = Inter({ subsets: ["latin"], variable: "--font-inter", display: "swap" });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });
// body className: `${inter.variable} ${geistMono.variable} antialiased`
// ThemeProvider defaultTheme="dark"
```

From app/src/app/globals.css:
```css
@theme inline { --font-sans: var(--font-inter), system-ui, sans-serif; }
:root { --primary: #7c3aed; --background: #fafafa; --sidebar: #09090b; }
.dark { --primary: #8b5cf6; --background: #09090b; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace fonts in layout.tsx</name>
  <files>app/src/app/layout.tsx</files>
  <action>
  1. Replace font imports: Remove `Inter` and `Geist_Mono`. Import `Playfair_Display` and `Source_Sans_3` from `next/font/google`.

  2. Configure fonts:
     ```typescript
     const playfairDisplay = Playfair_Display({
       subsets: ["latin"],
       variable: "--font-display",
       display: "swap",
       weight: ["400", "600", "700", "800", "900"],
     });

     const sourceSans3 = Source_Sans_3({
       subsets: ["latin"],
       variable: "--font-body",
       display: "swap",
       weight: ["300", "400", "600", "700"],
     });
     ```

  3. Update body className to: `${playfairDisplay.variable} ${sourceSans3.variable} antialiased`

  4. Change ThemeProvider `defaultTheme` from `"dark"` to `"light"` (per CONTEXT.md: light mode as default).

  5. Update metadata title to "No BS Homes" (or keep "HouseFinder" if brand change is just visual — use "HouseFinder" to be safe, this is the internal tool name).
  </action>
  <verify>
    <automated>cd app && grep "Playfair_Display" src/app/layout.tsx && grep "Source_Sans_3" src/app/layout.tsx && grep 'defaultTheme="light"' src/app/layout.tsx</automated>
  </verify>
  <done>layout.tsx loads Playfair Display + Source Sans 3 via next/font/google; defaultTheme is "light"</done>
</task>

<task type="auto">
  <name>Task 2: Replace globals.css with warm brand tokens</name>
  <files>app/src/app/globals.css</files>
  <action>
  Rewrite globals.css to replace the zinc/violet palette with the No BS Homes warm palette. Keep the same structural pattern (imports, @theme inline, :root, .dark, @layer base, component classes, animations).

  **@theme inline section** — update font references:
  - `--font-sans: var(--font-body), "Segoe UI", system-ui, sans-serif;` (Source Sans 3 for body)
  - Add: `--font-display: var(--font-display), Georgia, serif;` (Playfair Display for headings — note: the CSS var and the @theme key have the same name; Tailwind v4 handles this)
  - Keep all existing shadow, radius, and color-* mappings but they will read from the new :root values

  **:root (light mode)** — replace ALL values with warm palette:
  ```css
  :root {
    --background: #fdfbf7;           /* warm base */
    --foreground: #1e293b;           /* slate-800 — readable on cream */
    --card: #ffffff;
    --card-foreground: #1e293b;
    --popover: #ffffff;
    --popover-foreground: #1e293b;
    --primary: #1e4d8c;              /* brand blue */
    --primary-foreground: #ffffff;
    --secondary: #faf5ec;            /* cream */
    --secondary-foreground: #44403c; /* stone-700 */
    --muted: #f0dcc0;               /* sand-200 */
    --muted-foreground: #78716c;    /* stone-500 */
    --accent: #faf5ec;              /* cream */
    --accent-foreground: #44403c;
    --destructive: #dc2626;
    --border: #e5c79a;              /* sand-300 */
    --input: #e5c79a;
    --ring: #1e4d8c;                /* brand blue */
    --chart-1: #1e4d8c;            /* brand blue */
    --chart-2: #c4884f;            /* sand accent */
    --chart-3: #2d6cb4;            /* brand-400 */
    --chart-4: #d4a574;            /* sand-400 */
    --chart-5: #4a78c8;            /* brand-300 */
    --radius: 0.625rem;
    --sidebar: #fdfbf7;            /* warm base — light sidebar per CONTEXT */
    --sidebar-foreground: #1e293b;
    --sidebar-primary: #1e4d8c;
    --sidebar-primary-foreground: #ffffff;
    --sidebar-accent: #faf5ec;
    --sidebar-accent-foreground: #1e293b;
    --sidebar-border: #e5c79a;
    --sidebar-ring: #1e4d8c;
  }
  ```

  **.dark section** — warm navy dark mode:
  ```css
  .dark {
    --background: #0c1e38;          /* deep navy */
    --foreground: #fdfbf7;          /* warm white */
    --card: #122e54;                /* navy-700 */
    --card-foreground: #f9efe0;     /* sand-100 */
    --popover: #183d70;             /* navy-600 */
    --popover-foreground: #f9efe0;
    --primary: #4a78c8;             /* lighter blue for dark bg */
    --primary-foreground: #ffffff;
    --secondary: #183d70;           /* navy-600 */
    --secondary-foreground: #e5c79a; /* sand-300 */
    --muted: #183d70;
    --muted-foreground: #e5c79a;
    --accent: #183d70;
    --accent-foreground: #f9efe0;
    --destructive: #ef4444;
    --border: #183d70;
    --input: #183d70;
    --ring: #4a78c8;
    --chart-1: #4a78c8;
    --chart-2: #d4a574;
    --chart-3: #6b9ae0;
    --chart-4: #e5c79a;
    --chart-5: #8bb4ee;
    --sidebar: #060f1c;             /* brand-900 */
    --sidebar-foreground: #f9efe0;
    --sidebar-primary: #4a78c8;
    --sidebar-primary-foreground: #ffffff;
    --sidebar-accent: #0c1e38;
    --sidebar-accent-foreground: #f9efe0;
    --sidebar-border: #122e54;
    --sidebar-ring: #4a78c8;
  }
  ```

  **Grain overlay** — add after the .dark block, before @layer base:
  ```css
  /* Noise grain overlay — matches nobshomes */
  body::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 9999;
    opacity: 0.015;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  ```

  **Component classes** — update to use warm palette:
  - `.btn-primary`: Change to sand gradient: `bg-gradient-to-r from-[#c4884f] to-[#d4a574] hover:from-[#b37a45] hover:to-[#c4884f] text-white` (sand gradient for primary actions per CONTEXT)
  - `.btn-dark`: Change to brand blue: `bg-[#1e4d8c] text-white hover:bg-[#183d70]`
  - `.btn-outline-primary`: Update border/text to brand blue
  - `.card-elevated`: Add `rounded-2xl` (per CONTEXT: rounded-2xl cards)
  - `.card-surface`: Add `rounded-2xl`
  - `.hot-pulse`: Update gradient from red/violet to red/sand

  **Keep all animations** (fadeIn, fadeInUp, slideInLeft, stagger-*, scrollbar-hide) — they are palette-independent.

  **Add brand color utilities** for direct use:
  ```css
  @theme inline {
    /* Brand scale for direct use */
    --color-brand-50: #eef3fb;
    --color-brand-100: #d0dff5;
    --color-brand-500: #1e4d8c;
    --color-brand-700: #122e54;
    --color-brand-900: #060f1c;
    --color-sand-50: #fdf9f3;
    --color-sand-200: #f0dcc0;
    --color-sand-300: #e5c79a;
    --color-sand-500: #c4884f;
    --color-sand-700: #8b5a2f;
    --color-warm: #fdfbf7;
    --color-cream: #faf5ec;
  }
  ```
  </action>
  <verify>
    <automated>cd app && grep "#1e4d8c" src/app/globals.css && grep "#c4884f" src/app/globals.css && grep "grain" src/app/globals.css && grep "fdfbf7" src/app/globals.css && grep -v "7c3aed" src/app/globals.css | head -1</automated>
  </verify>
  <done>globals.css uses warm brand palette (blue/sand/cream) for both light and dark modes; grain overlay present; no zinc/violet colors remain; rounded-2xl on cards</done>
</task>

</tasks>

<verification>
- `cd app && npx next build` succeeds (fonts resolve, CSS compiles)
- No references to `#7c3aed` (old violet) or `#8b5cf6` in globals.css
- Brand blue `#1e4d8c` appears as --primary in :root
- Sand `#c4884f` appears in button gradients
- Grain overlay SVG present in body::before
- Light mode is default theme
</verification>

<success_criteria>
Design system foundation is in place: warm fonts load via next/font/google, all CSS tokens are warm palette, grain overlay renders, light mode is default. All downstream components inherit new colors without individual changes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-netlify-migration-design-system/17-02-SUMMARY.md`
</output>
