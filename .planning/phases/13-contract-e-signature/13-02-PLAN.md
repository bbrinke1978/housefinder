---
phase: 13-contract-e-signature
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/src/app/(dashboard)/deals/[id]/page.tsx
  - app/src/components/contract-tab.tsx
  - app/src/components/contract-list-item.tsx
  - app/src/components/contract-status-badge.tsx
  - app/src/components/contract-create-form.tsx
  - app/src/components/contract-clause-editor.tsx
  - app/src/app/(dashboard)/contracts/page.tsx
  - app/src/components/sidebar.tsx
autonomous: true
requirements:
  - CONTRACT-05
  - CONTRACT-06
  - CONTRACT-07
  - CONTRACT-08
  - CONTRACT-10

must_haves:
  truths:
    - "User can create a contract from the deal detail Financials tab with auto-filled deal data"
    - "User can edit clauses (add/remove/modify) before sending a contract"
    - "Contract list on deal detail shows status, type, parties, and action buttons"
    - "Global contracts page shows all contracts across all deals"
    - "Contracts link appears in sidebar navigation"
  artifacts:
    - path: "app/src/components/contract-tab.tsx"
      provides: "Contract list + create button for deal detail Financials tab"
      min_lines: 50
    - path: "app/src/components/contract-create-form.tsx"
      provides: "Contract creation form with auto-fill and clause editor"
      min_lines: 100
    - path: "app/src/components/contract-clause-editor.tsx"
      provides: "Editable clause list with add/remove/modify"
      min_lines: 60
    - path: "app/src/app/(dashboard)/contracts/page.tsx"
      provides: "Global contracts overview page"
      min_lines: 30
  key_links:
    - from: "app/src/components/contract-tab.tsx"
      to: "app/src/lib/contract-queries.ts"
      via: "getDealContracts for listing"
      pattern: "getDealContracts"
    - from: "app/src/components/contract-create-form.tsx"
      to: "app/src/lib/contract-actions.ts"
      via: "createContract server action"
      pattern: "createContract"
    - from: "app/src/app/(dashboard)/deals/[id]/page.tsx"
      to: "app/src/components/contract-tab.tsx"
      via: "ContractTab rendered in Financials tab"
      pattern: "ContractTab"
---

<objective>
Contract management UI: creation form with clause editor, contract listing on deal detail, global contracts page, and navigation.

Purpose: Give the user the ability to create, view, and manage contracts from both deal detail and a global overview page.
Output: Contract tab on deal detail, contract create form, clause editor, global contracts page, sidebar nav update.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-contract-e-signature/13-CONTEXT.md
@.planning/phases/13-contract-e-signature/13-RESEARCH.md
@.planning/phases/13-contract-e-signature/13-01-SUMMARY.md

@app/src/app/(dashboard)/deals/[id]/page.tsx
@app/src/components/sidebar.tsx
@app/src/types/index.ts
@app/src/lib/contract-queries.ts
@app/src/lib/contract-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract tab on deal detail + create form with clause editor</name>
  <files>
    app/src/app/(dashboard)/deals/[id]/page.tsx
    app/src/components/contract-tab.tsx
    app/src/components/contract-list-item.tsx
    app/src/components/contract-status-badge.tsx
    app/src/components/contract-create-form.tsx
    app/src/components/contract-clause-editor.tsx
  </files>
  <action>
    1. Create contract-status-badge.tsx: A simple Badge component that maps contract lifecycle status to color variants. Draft = outline, Sent = secondary (blue-ish), seller_signed = secondary, countersigned = default, executed = default (green), expired = destructive, voided = destructive, amended = outline. Use existing Badge component from ui/badge.

    2. Create contract-clause-editor.tsx: "use client" component.
       Props: clauses: ContractClause[], onChange: (clauses: ContractClause[]) => void
       Renders an ordered list of clauses. Each clause shows title (editable input) and body (editable textarea). Buttons: remove clause (trash icon), move up/down (arrow icons). "Add Clause" button at bottom creates a new clause with empty title/body and next order number. Use lucide-react icons (Trash2, ChevronUp, ChevronDown, Plus). Tailwind styling consistent with existing form patterns (border, rounded-lg, p-4, space-y-3).

    3. Create contract-create-form.tsx: "use client" component.
       Props: deal (DealWithBuyer from types — has address, city, state, sellerName, etc.), onClose: () => void
       - Contract type selector: two buttons (Purchase Agreement / Assignment)
       - Auto-fill fields from deal: propertyAddress=deal.address, city=deal.city, county=deal.county, parcelId (if available), sellerName=deal.sellerName, purchasePrice=deal.offerPrice, arv=deal.arv, assignmentFee=deal.assignmentFee
       - For Purchase Agreement: signer 1 = seller (name from deal.sellerName, email input required), signer 2 = wholesaler (Brian — pre-fill from mail settings or leave for user input)
       - For Assignment: signer 1 = wholesaler (Brian), signer 2 = buyer (name/email inputs)
       - buyerName field (editable, populated from deal.buyerName if assigned)
       - earnestMoney (default 100), inspectionPeriodDays (default 10), closingDays (default 30) — editable number inputs
       - Clause editor section: initialize with DEFAULT_PURCHASE_CLAUSES or DEFAULT_ASSIGNMENT_CLAUSES based on type selection. Use ContractClauseEditor component.
       - Submit calls createContract server action with FormData. On success, close form. On error, show error toast or inline message.
       - Use useTransition for pending state on submit button (consistent with Phase 12 pattern).

    4. Create contract-list-item.tsx: Renders a single contract row.
       Shows: contract type label, status badge, counterparty name (seller or buyer), created date. Action buttons: "Send" (if draft — calls sendForSigning), "Preview PDF" (download link to /api/contracts/[id]/pdf), "Void" (if not executed/voided), "Resend" (if sent and pending signature). Use useTransition for action buttons.

    5. Create contract-tab.tsx: Server component wrapper.
       Props: dealId, deal (for auto-fill into create form)
       Fetches contracts via getDealContracts(dealId). Renders contract count header, a "New Contract" button that toggles showing ContractCreateForm, and a list of ContractListItem components. If no contracts, show empty state with prompt to create first contract.

    6. Update deal detail page (deals/[id]/page.tsx):
       - Import ContractTab and getDealContracts (or getContractCountByDealId for badge)
       - In the Financials tab content, add a "Contracts" section ABOVE the existing "Budget & Expenses" section (contracts are more prominent than budget in the deal flow)
       - Pass deal and contracts data to ContractTab
       - Add contract count badge to the Financials tab trigger (similar to notes count on Activity tab)
  </action>
  <verify>
    cd app && npx tsc --noEmit && echo "TypeScript OK"
  </verify>
  <done>
    - ContractTab renders on deal detail Financials tab with contract list and "New Contract" button
    - ContractCreateForm auto-fills from deal data and allows clause editing before creation
    - ContractClauseEditor supports add/remove/modify/reorder of clauses
    - ContractListItem shows status badge and action buttons (Send, Preview PDF, Void, Resend)
    - Contract count badge appears on Financials tab trigger
  </done>
</task>

<task type="auto">
  <name>Task 2: Global contracts page + sidebar navigation</name>
  <files>
    app/src/app/(dashboard)/contracts/page.tsx
    app/src/components/sidebar.tsx
  </files>
  <action>
    1. Create contracts/page.tsx: Server component, force-dynamic.
       - Fetch all contracts via getAllContracts()
       - Render a page header "Contracts" with count
       - Group or filter by status: show active contracts (draft, sent, seller_signed, countersigned) at top, then executed, then expired/voided at bottom
       - Each row shows: deal address (link to deal detail), contract type, counterparty, status badge, dates (created, sent, executed), action buttons
       - If no contracts, show empty state: "No contracts yet. Create one from a deal's Financials tab."
       - Use existing table/list styling patterns from deals/page.tsx or campaigns/page.tsx

    2. Update sidebar.tsx:
       - Add "Contracts" link with FileText icon from lucide-react
       - Place it after "Deals" in the nav order (Deals → Contracts → Campaigns flow makes logical sense for the deal lifecycle)
       - Use the same active state pattern as other nav items
  </action>
  <verify>
    cd app && npx tsc --noEmit && echo "TypeScript OK"
  </verify>
  <done>
    - /contracts page renders all contracts across all deals grouped by status
    - Sidebar shows Contracts link with FileText icon between Deals and Campaigns
    - Clicking a contract row links to the deal detail page
  </done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes
- Deal detail Financials tab shows contract section with create button
- /contracts route exists and renders
- Sidebar includes Contracts navigation item
</verification>

<success_criteria>
The user can create contracts from deal detail with auto-filled data and editable clauses, view contract status and take actions (send, void, resend), and browse all contracts from a global contracts page accessible via sidebar navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-e-signature/13-02-SUMMARY.md`
</output>
