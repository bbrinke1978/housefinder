---
phase: 25.5-utah-legals-slc-activation
plan: "02"
type: execute
wave: 2
depends_on:
  - "25.5-01"
files_modified:
  - scraper/src/scripts/dry-run-utah-legals-slc.ts
autonomous: false
requirements:
  - RP-09
  - RP-10
  - RP-11

must_haves:
  truths:
    - "Index 17 returns Salt Lake County notices — county field shows 'salt lake', not 'utah' or empty"
    - "At least some SLC notices have real hyphenated parcel IDs (not all synthetic ul- IDs)"
    - "SLC notices excluded by the allowlist are logged (Sandy/Midvale/Holladay etc. appear in log with 'excluded' message)"
    - "After the next scheduled scraper run, SELECT COUNT(*) FROM properties WHERE city='Rose Park' returns > 0 OR absence is documented with dry-run evidence"
    - "Rose Park properties in DB have distress_score > 0 and appear in dashboard Rose Park filter"
  artifacts:
    - path: ".planning/phases/25.5-utah-legals-slc-activation/25.5-02-SUMMARY.md"
      provides: "Dry-run output, index confirmation, parcel extraction stats, production run result"
  key_links:
    - from: "scraper/src/sources/utah-legals.ts"
      to: "properties table"
      via: "upsertFromUtahLegals() -> upsertProperty() -> normalizeCity('84116') -> city='Rose Park'"
      pattern: "city.*Rose Park.*84116"
---

<objective>
Run the Phase 25.5 dry-run script against the live Utah Legals site to confirm the three implementation assumptions, then verify production DB state after the next scheduled scraper run.

Purpose: The dry-run is a mandatory safety gate — it confirms index 17 selects Salt Lake County only (not the combined "Salt Lake/Utah" option at index 18), validates parcel ID extraction on real SLC notice snippets, and logs the city values actually seen so the allowlist can be adjusted if needed. Only after the human confirms the dry-run output does production scraping proceed.

Output: Dry-run log reviewed and confirmed, production DB has Rose Park rows (or absence documented), SUMMARY written.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.5-utah-legals-slc-activation/25.5-RESEARCH.md
@.planning/phases/25.5-utah-legals-slc-activation/25.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dry-run script and capture diagnostic output</name>
  <files>scraper/src/scripts/dry-run-utah-legals-slc.ts</files>
  <action>
Run the dry-run script against the live Utah Legals site. This makes NO database writes. Capture and display the full console output for human review.

First, check if ts-node is available in the scraper package. If not, compile first then run the JS output.

```bash
# Option A: if ts-node available in scraper/
cd /c/Users/brian.BRIANRIPPER/OneDrive/Documents/housefinder/scraper
npx ts-node --esm src/scripts/dry-run-utah-legals-slc.ts 2>&1 | tee /tmp/slc-dry-run.txt

# Option B: if ts-node not available, compile and run
cd /c/Users/brian.BRIANRIPPER/OneDrive/Documents/housefinder/scraper
npx tsc && node dist/scripts/dry-run-utah-legals-slc.js 2>&1 | tee /tmp/slc-dry-run.txt
```

After the run completes, display the full output. Pay attention to:

1. **County values line** — PASS if "salt lake" appears; FAIL/WARNING if "utah" also appears (means index 18 was actually selected)
2. **SLC notice count** — 0 means either no active SLC NODs this week (acceptable) or wrong index (check county values line)
3. **Parcel extraction stats** — "Real parcel IDs: N" (want N > 0); if N = 0 and synthetic count is high, Branch 2 and 4 are not matching and the snippet may not contain parcel labels
4. **City values in SLC notices** — lists what cities appeared in the allowlist-passed notices
5. **Excluded messages in log** — confirm Sandy/Midvale/etc. are being excluded

If the run fails with a Playwright/Chromium error, check if Chromium is installed: `cd scraper && npx playwright install chromium`. If it fails with a network error, note it in the checkpoint — the scraper requires live internet access to utahlegals.com.

Display the full output from /tmp/slc-dry-run.txt as part of this task's output.
  </action>
  <verify>cat /tmp/slc-dry-run.txt | grep -E "SUMMARY|County values|SLC notices|parcel" | head -20</verify>
  <done>
Dry-run output displayed in full. Three key lines present:
- "County values seen" line (used to verify index 17 is correct)
- "SLC notices (after allowlist)" line with a count
- "SLC parcel extraction" section with real vs synthetic counts
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies dry-run output — index 17 correct, parcel IDs extracting</name>
  <what-built>
Dry-run script ran against live utahlegals.com. The full output is displayed above in Task 1. Claude will paste the key diagnostic lines below this task for quick review.

Key things to confirm:
1. "County values seen" does NOT include "utah" (would mean wrong index was selected)
2. SLC notice count makes sense (could be 0 if no active 84116 NODs this week — that is OK)
3. Parcel IDs: if "Real parcel IDs: 0" appears, note it — the executor will investigate Branch 2/4 matching and may need to inspect the actual snippet text to adjust the regex
4. Any unexpected city values not in the allowlist that should be included
  </what-built>
  <how-to-verify>
Review the dry-run output displayed by Task 1. Check for:

PASS signals:
- County values: "salt lake" present, "utah" NOT present
- Excluded messages visible in log for Sandy/Midvale type cities (or no excluded messages if there were no SLC NODs outside 84116)
- If SLC notices > 0: at least some real parcel IDs extracted

FAIL signals requiring action before proceeding:
- "utah" appears in County values → index 17 selected combined "Salt Lake/Utah" county; must switch to a different index and re-run
- All SLC notices have synthetic ul- IDs → Branch 2 and 4 not matching snippet text; need to inspect actual snippet and adjust regex
- "salt lake/utah" appears as county name → same index issue

ACCEPTABLE (not a failure):
- SLC notices count = 0 (no active 84116 NODs in the current Utah Legals publication window)
- A few Sandy/Midvale exclusions logged (filter is working correctly)
  </how-to-verify>
  <action>Review dry-run output from Task 1. Confirm county values exclude "utah", parcel extraction is working, and city allowlist is filtering correctly. If issues found, Claude will fix before production run.</action>
  <files></files>
  <verify>Human confirms "confirmed" or describes issues for Claude to fix</verify>
  <done>Index 17 confirmed as Salt Lake County only; parcel extraction validated; allowlist behavior understood</done>
  <resume-signal>Type "confirmed" if index/parcel/city output looks correct. Or describe any issues (wrong county, all-synthetic IDs, unexpected cities) so Claude can fix before production.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify production DB state after next scraper run</name>
  <files></files>
  <action>
After human confirms the dry-run output (or any corrections from checkpoint feedback are applied), determine whether the production scraper has run since Plan 01 was deployed.

Check the Azure Function timer trigger schedule — scraper runs daily at 5 AM MT. If it has not yet run since Plan 01 deployment, trigger it manually OR wait for the next scheduled run.

**Option A: Check if scraper has already run (check scraper_health table)**
```sql
SELECT scraper_name, last_run_at, last_result
FROM scraper_health
WHERE scraper_name = 'utah-legals'
ORDER BY last_run_at DESC
LIMIT 1;
```
Run via: `psql $DATABASE_URL -c "SELECT ..."`

If `last_run_at` is AFTER the Plan 01 commit timestamp (2026-04-26 or later), the scraper already ran with the new code.

**Option B: Query DB for Rose Park rows**
```sql
SELECT COUNT(*) as rose_park_count
FROM properties
WHERE city = 'Rose Park';

SELECT p.address, p.parcel_id, p.distress_score, ds.signal_type
FROM properties p
JOIN distress_signals ds ON ds.property_id = p.id
WHERE p.city = 'Rose Park'
ORDER BY p.created_at DESC
LIMIT 10;
```

**Option C: Check dashboard**
Navigate to the live dashboard URL and filter by city = "Rose Park". If rows appear, success criteria 1 and 4 are met.

**Document the result:**
- If count > 0: Success. Note the count and sample parcel IDs in the task output.
- If count = 0 AND scraper has not run yet: Note that the scraper must run first; this is expected.
- If count = 0 AND scraper ran after code deploy: Check scraper logs for "[utah-legals] SLC" lines. Possible explanations: no active SLC NODs published this week (legitimate), or allowlist excluded all SLC notices (check exclusion log messages).

Either outcome is valid for Phase 25.5 completion — the success criteria explicitly allows "zero with documented evidence."
  </action>
  <verify>psql $DATABASE_URL -c "SELECT COUNT(*) FROM properties WHERE city='Rose Park';" 2>/dev/null || echo "DB check — document result from Azure portal or Netlify environment"</verify>
  <done>
One of:
(a) Rose Park row count > 0 — first real Rose Park properties in DB
(b) Row count = 0 with documented evidence: scraper ran and logged its SLC behavior, or no SLC NODs were published in the current Utah Legals window
Phase 25.5 success criteria are met either way if the documentation is present.
  </done>
</task>

</tasks>

<verification>
Phase 25.5 complete when ALL of the following are true:

1. Dry-run confirmed: index 17 returns Salt Lake County notices (not "utah" combined)
2. Dry-run confirmed: parcel extraction working or documented as snippet-truncated (branch investigation noted for future)
3. DB query result documented: either Rose Park rows exist OR absence is explained with scraper log evidence
4. If Rose Park rows exist: `SELECT distress_score FROM properties WHERE city='Rose Park' LIMIT 5` shows score > 0
5. Dashboard Rose Park filter shows rows (or "no results" with documented explanation)
</verification>

<success_criteria>
Phase 25.5 is complete when:
- Code from Plan 01 is deployed and the next utah-legals.ts run uses the new SLC configuration
- The dry-run confirmed index 17 selects Salt Lake County notices, not the combined "Salt Lake/Utah" option
- DB state is documented: either Rose Park rows exist with distress_score > 0, or absence is explained by scraper log evidence (no SLC NODs published this week)
- SUMMARY.md written with dry-run output, parcel extraction stats, and DB result
</success_criteria>

<output>
After completion, create `.planning/phases/25.5-utah-legals-slc-activation/25.5-02-SUMMARY.md`
</output>
