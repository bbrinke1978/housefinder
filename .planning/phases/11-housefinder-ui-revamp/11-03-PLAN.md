---
phase: 11-housefinder-ui-revamp
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/src/components/deal-kanban.tsx
  - app/src/app/(dashboard)/deals/page.tsx
  - app/src/components/deal-card.tsx
  - app/src/components/deal-list.tsx
  - app/src/app/(dashboard)/analytics/page.tsx
  - app/src/components/analytics-funnel.tsx
  - app/src/components/analytics-market.tsx
  - app/src/components/analytics-trends.tsx
  - app/src/components/analytics-attribution.tsx
  - app/src/components/analytics-outreach.tsx
  - app/src/components/analytics-scraper-health.tsx
  - app/src/components/analytics-activity-log.tsx
  - app/src/components/analytics-info-panel.tsx
  - app/src/app/(dashboard)/map/page.tsx
autonomous: true
requirements: [UI-05, UI-07]

must_haves:
  truths:
    - "Deals kanban has horizontal scroll on mobile and defaults to list view on mobile screens"
    - "Analytics charts are responsive and readable on a 360px screen"
    - "All inner pages use the new violet/zinc palette with no old color references"
    - "Deal cards use semantic tokens and feel consistent with the new dashboard design"
  artifacts:
    - path: "app/src/components/deal-kanban.tsx"
      provides: "Horizontally scrollable kanban with mobile optimization"
      contains: "overflow-x-auto"
    - path: "app/src/app/(dashboard)/analytics/page.tsx"
      provides: "Analytics page with updated palette"
  key_links:
    - from: "app/src/app/(dashboard)/deals/page.tsx"
      to: "app/src/components/deal-kanban.tsx"
      via: "Kanban component with mobile scroll"
      pattern: "DealKanban"
---

<objective>
Refresh all inner pages: deals (kanban mobile treatment), analytics (responsive charts), and map styling to use the new design system.

Purpose: After the foundation (Plan 01) and dashboard (Plan 02), these pages still have old palette references. This plan brings all remaining list/chart pages up to the new design standard.
Output: Every inner page uses the new palette consistently and works on mobile.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-housefinder-ui-revamp/11-CONTEXT.md
@.planning/phases/11-housefinder-ui-revamp/11-RESEARCH.md
@.planning/phases/11-housefinder-ui-revamp/11-01-SUMMARY.md

@app/src/components/deal-kanban.tsx
@app/src/app/(dashboard)/deals/page.tsx
@app/src/components/deal-card.tsx
@app/src/components/deal-list.tsx
@app/src/app/(dashboard)/analytics/page.tsx
@app/src/components/analytics-funnel.tsx
@app/src/components/analytics-market.tsx
@app/src/components/analytics-trends.tsx
@app/src/app/(dashboard)/map/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deals page — kanban mobile scroll, deal cards, and list view refresh</name>
  <files>app/src/components/deal-kanban.tsx, app/src/app/(dashboard)/deals/page.tsx, app/src/components/deal-card.tsx, app/src/components/deal-list.tsx</files>
  <action>
**deal-kanban.tsx — Mobile horizontal scroll:**

1. Wrap the kanban column container in a horizontal scroll wrapper:
   ```tsx
   <div className="overflow-x-auto -mx-4 px-4 md:mx-0 md:px-0 pb-4 overscroll-x-contain">
     <div className="flex gap-3" style={{ minWidth: "max-content" }}>
       {/* Each column gets min-w-[260px] max-w-[300px] flex-shrink-0 */}
     </div>
   </div>
   ```

2. The `DragDropContext` should remain OUTSIDE the scroll wrapper — only the visual columns go inside.

3. Update all column and card colors from old palette to semantic tokens:
   - Column backgrounds: `bg-card` or `bg-muted`
   - Column headers: `text-foreground` with `border-b border-border`
   - Count badges: `bg-muted text-muted-foreground`

4. Add scrollbar-hide class to the overflow container (same pattern as stats bar)

**deals/page.tsx:**

1. Update the view toggle buttons to use semantic tokens
2. Replace any old color classes
3. Add a mobile hint when kanban is active: a small `text-xs text-muted-foreground` note like "Scroll horizontally to see all columns" visible only on `md:hidden`
4. The existing `view` searchParam toggle (kanban/list) should be preserved — no behavioral changes

**deal-card.tsx:**

1. Replace all old color references with semantic tokens
2. Status badges: use `bg-primary/10 text-primary` for active statuses, `bg-muted text-muted-foreground` for neutral
3. Update any brand-*, warm-*, dark-* references
4. Tighten mobile padding if needed (p-3 mobile, p-4 desktop)

**deal-list.tsx:**

1. Replace old color references with semantic tokens
2. Table/list rows: `hover:bg-accent transition-colors`
3. Ensure touch targets are 44px minimum height per row
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Kanban columns scroll horizontally on mobile with overscroll containment
    - DragDropContext remains outside scroll container
    - All deal components use semantic color tokens
    - View toggle (kanban/list) works as before
    - No old palette references in any modified file
  </done>
</task>

<task type="auto">
  <name>Task 2: Analytics page and chart components — responsive sizing and palette refresh</name>
  <files>app/src/app/(dashboard)/analytics/page.tsx, app/src/components/analytics-funnel.tsx, app/src/components/analytics-market.tsx, app/src/components/analytics-trends.tsx, app/src/components/analytics-attribution.tsx, app/src/components/analytics-outreach.tsx, app/src/components/analytics-scraper-health.tsx, app/src/components/analytics-activity-log.tsx, app/src/components/analytics-info-panel.tsx, app/src/app/(dashboard)/map/page.tsx</files>
  <action>
**Analytics page and ALL analytics components:**

This is a sweep task — update every analytics component's color references. For each file:

1. Replace all `brand-*`, `warm-*`, `dark-*` color references with semantic tokens:
   - `bg-warm-*` / `bg-dark-*` backgrounds → `bg-card`, `bg-muted`, `bg-background`
   - `text-dark-*` → `text-foreground`, `text-muted-foreground`
   - `border-warm-*` / `border-dark-*` → `border-border`
   - `text-brand-*` → `text-primary`

2. For Recharts chart components, update the color arrays to use violet palette:
   - Primary series: `#8b5cf6` (violet-500)
   - Secondary series: `#a78bfa` (violet-400)
   - Tertiary: `#6d28d9` (violet-700)
   - Quaternary: `#c4b5fd` (violet-300)
   - Additional: `#ddd6fe` (violet-200)
   - Keep semantic colors where applicable (emerald for positive, red for negative)

3. Ensure all chart containers use `ResponsiveContainer width="100%" height={...}` — if any use fixed widths, switch to responsive.

4. Set minimum chart height to 200px on mobile (some may be taller on desktop)

5. Tab navigation on analytics page: update active tab style to use `text-primary border-primary` instead of any brand colors.

6. Card containers around charts: use `bg-card border border-border rounded-xl p-4`

**map/page.tsx:**

1. Replace any old color references (this page is mostly a Mapbox embed so changes may be minimal)
2. Update the map page header/controls if they use old palette colors
3. The map popup card styling should use `bg-card border-border text-foreground`
4. Update any filter controls on the map page to match the new palette

**IMPORTANT:** Do NOT change Recharts import patterns or data fetching logic. Only visual changes.
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All analytics components use semantic color tokens
    - Recharts charts use violet palette series colors
    - Charts are responsive (ResponsiveContainer) and readable at 360px
    - Map page controls and popups use new palette
    - No brand-*, warm-*, dark-* references in any modified file
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `grep -rn "brand-500\|warm-50\|warm-200\|dark-950\|dark-700\|dark-400" app/src/components/deal-kanban.tsx app/src/components/deal-card.tsx app/src/components/analytics-*.tsx app/src/app/\(dashboard\)/deals/ app/src/app/\(dashboard\)/analytics/ app/src/app/\(dashboard\)/map/` returns zero matches
- `npx next build` succeeds
- Kanban scrolls horizontally on mobile
</verification>

<success_criteria>
All inner pages (deals, analytics, map) are visually updated to the new zinc/violet palette. The kanban is usable on mobile via horizontal scroll. Charts are responsive and use the violet color series. No old palette remnants exist in any modified file.
</success_criteria>

<output>
After completion, create `.planning/phases/11-housefinder-ui-revamp/11-03-SUMMARY.md`
</output>
