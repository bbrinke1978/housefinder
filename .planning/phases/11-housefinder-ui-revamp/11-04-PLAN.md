---
phase: 11-housefinder-ui-revamp
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/src/app/(dashboard)/properties/[id]/page.tsx
  - app/src/components/property-overview.tsx
  - app/src/components/signal-timeline.tsx
  - app/src/components/lead-notes.tsx
  - app/src/components/contact-tab.tsx
  - app/src/app/(dashboard)/deals/[id]/page.tsx
  - app/src/components/deal-overview.tsx
  - app/src/components/deal-mao-calculator.tsx
  - app/src/components/deal-contract-tracker.tsx
  - app/src/components/deal-notes.tsx
  - app/src/components/deal-blast-generator.tsx
  - app/src/components/budget-tab.tsx
  - app/src/components/budget-category-editor.tsx
  - app/src/components/budget-charts.tsx
  - app/src/components/budget-alerts.tsx
  - app/src/components/field-observations.tsx
  - app/src/components/deal-comp-entry.tsx
  - app/src/components/deal-guide-panel.tsx
  - app/src/app/(dashboard)/pipeline/page.tsx
  - app/src/components/lead-kanban.tsx
  - app/src/components/lead-list.tsx
  - app/src/components/lead-card.tsx
  - app/src/app/(dashboard)/settings/page.tsx
  - app/src/components/settings-form.tsx
autonomous: true
requirements: [UI-05, UI-07]

must_haves:
  truths:
    - "Property detail page uses semantic color tokens with no old palette references"
    - "Deal detail page tabs/sections are readable on mobile"
    - "Pipeline page (lead kanban/list) uses new palette"
    - "Settings page uses new palette"
    - "All detail page components use consistent card/border/text styling"
  artifacts:
    - path: "app/src/app/(dashboard)/properties/[id]/page.tsx"
      provides: "Property detail with new palette"
    - path: "app/src/app/(dashboard)/deals/[id]/page.tsx"
      provides: "Deal detail with new palette"
  key_links:
    - from: "app/src/app/(dashboard)/properties/[id]/page.tsx"
      to: "app/src/components/property-overview.tsx"
      via: "Tab content component"
      pattern: "PropertyOverview"
---

<objective>
Refresh all detail pages (property detail, deal detail) and remaining pages (pipeline, settings) to use the new design system.

Purpose: These are the deep-dive pages users spend time on. They need consistent styling with the dashboard and inner pages updated in Plans 01-03.
Output: Every page in the app uses the new violet/zinc palette with zero remnants of the old terracotta/desert theme.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-housefinder-ui-revamp/11-CONTEXT.md
@.planning/phases/11-housefinder-ui-revamp/11-RESEARCH.md
@.planning/phases/11-housefinder-ui-revamp/11-01-SUMMARY.md

@app/src/app/(dashboard)/properties/[id]/page.tsx
@app/src/components/property-overview.tsx
@app/src/components/signal-timeline.tsx
@app/src/components/contact-tab.tsx
@app/src/app/(dashboard)/deals/[id]/page.tsx
@app/src/components/deal-overview.tsx
@app/src/components/deal-mao-calculator.tsx
@app/src/components/deal-contract-tracker.tsx
@app/src/components/budget-tab.tsx
@app/src/app/(dashboard)/pipeline/page.tsx
@app/src/components/lead-kanban.tsx
@app/src/components/lead-card.tsx
@app/src/app/(dashboard)/settings/page.tsx
@app/src/components/settings-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property detail and pipeline pages — palette sweep and mobile improvements</name>
  <files>app/src/app/(dashboard)/properties/[id]/page.tsx, app/src/components/property-overview.tsx, app/src/components/signal-timeline.tsx, app/src/components/lead-notes.tsx, app/src/components/contact-tab.tsx, app/src/app/(dashboard)/pipeline/page.tsx, app/src/components/lead-kanban.tsx, app/src/components/lead-list.tsx, app/src/components/lead-card.tsx</files>
  <action>
**This is a color sweep task.** For every file listed, replace ALL old palette references with semantic tokens. The pattern is consistent:

**Search and replace in every file:**
- `bg-warm-*` → `bg-card`, `bg-muted`, or `bg-background` as appropriate
- `text-dark-*` → `text-foreground` or `text-muted-foreground`
- `border-warm-*` / `border-dark-*` → `border-border`
- `text-brand-*` / `bg-brand-*` → `text-primary` / `bg-primary`
- `dark:bg-dark-*` → remove (semantic tokens handle dark mode automatically)
- `dark:text-dark-*` → remove
- `dark:border-dark-*` → remove
- `card-warm` → `bg-card border border-border rounded-xl` (or `card-elevated` class)
- `font-display` / `font-heading` → remove (Inter handles all weights)

**property detail page specifics:**
- Tab headers: `text-foreground` active, `text-muted-foreground` inactive, `border-primary` active indicator
- Signal timeline icons: keep semantic colors (red for NOD, amber for tax, etc.) but update container/text to semantic tokens
- Contact tab: phone links remain `text-primary` (violet), update any old brand-* coloring
- Notes tab: note cards use `bg-muted border-border`, timestamps `text-muted-foreground`

**pipeline page specifics:**
- Same kanban horizontal scroll pattern as deals if applicable — check if lead-kanban has the same issues
- Lead cards: same pattern as property cards — compact, semantic tokens
- Status badges: `bg-primary/10 text-primary` for active, `bg-muted text-muted-foreground` for neutral

**IMPORTANT:** Do NOT change any data fetching, form submission, or business logic. This is purely visual.
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Property detail page uses semantic tokens throughout all tabs
    - Pipeline page uses new palette
    - Lead cards match the new design system
    - No brand-*, warm-*, dark-* references in any modified file
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Deal detail, budget, and settings pages — palette sweep</name>
  <files>app/src/app/(dashboard)/deals/[id]/page.tsx, app/src/components/deal-overview.tsx, app/src/components/deal-mao-calculator.tsx, app/src/components/deal-contract-tracker.tsx, app/src/components/deal-notes.tsx, app/src/components/deal-blast-generator.tsx, app/src/components/budget-tab.tsx, app/src/components/budget-category-editor.tsx, app/src/components/budget-charts.tsx, app/src/components/budget-alerts.tsx, app/src/components/field-observations.tsx, app/src/components/deal-comp-entry.tsx, app/src/components/deal-guide-panel.tsx, app/src/app/(dashboard)/settings/page.tsx, app/src/components/settings-form.tsx</files>
  <action>
**Same color sweep pattern as Task 1.** For every file:

1. Replace all `brand-*`, `warm-*`, `dark-*` color references with semantic tokens
2. Remove explicit `dark:` variants where semantic tokens handle both modes
3. Replace `card-warm` with `bg-card border border-border rounded-xl`
4. Replace `font-display` / `font-heading` references (Inter handles all weights)

**deal detail page specifics:**
- Tab system: same pattern as property detail — `text-foreground` active, `text-muted-foreground` inactive
- MAO calculator: keep green/red/amber semantic colors for profit/loss/breakeven indicators, but update container styling
- Contract tracker: step indicators use `bg-primary` for completed, `bg-muted` for pending
- Deal blast: code block / copy area uses `bg-muted border-border font-mono`

**budget components specifics:**
- Progress bars: keep green/yellow/red semantic colors for budget health
- Alert banners: keep destructive red for over-budget, update any brand-* to primary
- Charts: update Recharts colors to violet palette (same as analytics Plan 03)
- Category editor: form inputs use `bg-input border-border`

**settings page specifics:**
- Form inputs: `bg-input border-border text-foreground`
- Section headings: `text-foreground font-semibold`
- Save button: `bg-primary hover:bg-primary/90 text-primary-foreground`

**Final sweep:** After updating all files, run a comprehensive grep across the ENTIRE app/src directory:
```bash
grep -rn "brand-500\|brand-600\|warm-50\|warm-100\|warm-200\|warm-300\|dark-950\|dark-900\|dark-700\|dark-600\|dark-500\|dark-400\|dark-300\|dark-200\|dark-100\|card-warm\|card-photo\|font-display\|font-heading\|font-body\|bg-white\|text-white" app/src/
```

Fix ANY remaining old references found. The only acceptable `bg-white` or `text-white` should be where they are semantically correct (e.g., `text-white` on a colored background badge). Replace `bg-white` with `bg-background` or `bg-card` in all layout contexts.

**NOTE:** Some components may have been partially updated by Plan 01's global class rename. Check what Plan 01 summary says was updated and skip those. Focus on files NOT touched by Plans 01-03.
  </action>
  <verify>
    <automated>cd app && grep -rn "brand-500\|brand-600\|warm-50\|warm-100\|warm-200\|dark-950\|dark-700\|dark-400\|card-warm\|font-display\|font-heading" app/src/ | wc -l</automated>
  </verify>
  <done>
    - Deal detail page and all sub-components use semantic tokens
    - Budget components use new palette (keeping semantic green/yellow/red)
    - Settings page uses new palette
    - grep for old palette references across entire app/src returns 0 matches
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `grep -rn "brand-500\|warm-50\|warm-200\|dark-950\|dark-700\|dark-400\|card-warm\|font-display\|font-heading" app/src/` returns 0 lines
- `npx next build` succeeds
- All detail pages render consistently in dark mode
</verification>

<success_criteria>
Every component and page in the entire app uses the new violet/zinc semantic token system. Zero remnants of the old terracotta/desert theme exist anywhere in app/src. All pages render correctly in both dark (primary) and light modes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-housefinder-ui-revamp/11-04-SUMMARY.md`
</output>
