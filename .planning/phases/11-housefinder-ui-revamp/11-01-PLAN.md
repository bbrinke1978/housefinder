---
phase: 11-housefinder-ui-revamp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/globals.css
  - app/src/app/layout.tsx
  - app/src/app/(dashboard)/layout.tsx
  - app/src/components/app-sidebar.tsx
  - app/src/components/bottom-nav.tsx
  - app/src/app/login/page.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-06]

must_haves:
  truths:
    - "Dark mode shows zinc/slate neutral base with violet accent — no terracotta or desert amber visible"
    - "Inter font renders on all text — no Bebas Neue or Oswald visible anywhere"
    - "Desktop sidebar uses new palette colors with violet accent on active item"
    - "Mobile bottom nav uses new palette with violet active state and 44px min touch targets"
    - "Login page has dark premium aesthetic as first impression"
    - "Light mode uses clean zinc neutrals with violet accent — not warm sand"
  artifacts:
    - path: "app/src/app/globals.css"
      provides: "New color palette, font variables, updated component classes"
      contains: "--primary: #8b5cf6"
    - path: "app/src/app/layout.tsx"
      provides: "Inter font loaded via next/font/google"
      contains: "Inter"
    - path: "app/src/components/bottom-nav.tsx"
      provides: "Refreshed mobile bottom nav with new palette"
      contains: "text-primary"
  key_links:
    - from: "app/src/app/layout.tsx"
      to: "app/src/app/globals.css"
      via: "CSS variable --font-inter consumed by @theme inline --font-sans"
      pattern: "--font-inter"
    - from: "app/src/app/(dashboard)/layout.tsx"
      to: "app/src/app/globals.css"
      via: "bg-background class uses new --background variable"
      pattern: "bg-background"
---

<objective>
Design foundation: replace the terracotta/desert palette with a premium zinc + violet dark-first palette, swap to Inter font, and refresh the navigation shell and login page.

Purpose: This is the highest-leverage plan — changing globals.css instantly updates every page. Font + palette + nav shell establishes the entire new identity.
Output: Every page in HouseFinder renders with the new premium dark dashboard aesthetic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-housefinder-ui-revamp/11-CONTEXT.md
@.planning/phases/11-housefinder-ui-revamp/11-RESEARCH.md

@app/src/app/globals.css
@app/src/app/layout.tsx
@app/src/app/(dashboard)/layout.tsx
@app/src/components/app-sidebar.tsx
@app/src/components/bottom-nav.tsx
@app/src/app/login/page.tsx
@app/src/components/theme-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace color palette, font system, and component classes in globals.css + layout.tsx</name>
  <files>app/src/app/globals.css, app/src/app/layout.tsx</files>
  <action>
**DESIGN QUALITY NOTE:** This task sets the entire visual identity. Reference Linear.app and Vercel Dashboard for the intended quality level.

**globals.css — Full palette replacement:**

1. Delete the Google Fonts `@import url(...)` line (line 1) — fonts will load via next/font instead.

2. Update `@theme inline` block:
   - Change `--font-sans` to: `var(--font-inter), system-ui, sans-serif`
   - Remove `--font-display`, `--font-heading`, `--font-body` references
   - Remove entire brand palette block (`--color-brand-50` through `--color-brand-900`)
   - Remove entire warm neutrals block (`--color-warm-50` through `--color-warm-900`)
   - Remove `--font-display`, `--font-heading`, `--font-body` variable definitions
   - Remove `--shadow-warm`, `--shadow-warm-lg`, `--shadow-photo` — replace with:
     - `--shadow-subtle: 0 1px 2px rgba(0, 0, 0, 0.05)`
     - `--shadow-card: 0 2px 8px rgba(0, 0, 0, 0.08)`
   - Keep dark neutrals but rename to zinc scale (optional — or just replace with inline Tailwind zinc classes)

3. Replace `:root` (light mode) variables:
   ```
   --background: #fafafa;
   --foreground: #09090b;
   --card: #ffffff;
   --card-foreground: #09090b;
   --popover: #ffffff;
   --popover-foreground: #09090b;
   --primary: #7c3aed;
   --primary-foreground: #ffffff;
   --secondary: #f4f4f5;
   --secondary-foreground: #3f3f46;
   --muted: #f4f4f5;
   --muted-foreground: #71717a;
   --accent: #f4f4f5;
   --accent-foreground: #3f3f46;
   --destructive: #dc2626;
   --border: #e4e4e7;
   --input: #e4e4e7;
   --ring: #7c3aed;
   --chart-1: #8b5cf6;
   --chart-2: #6d28d9;
   --chart-3: #a78bfa;
   --chart-4: #4c1d95;
   --chart-5: #c4b5fd;
   --radius: 0.625rem;
   --sidebar: #09090b;
   --sidebar-foreground: #d4d4d8;
   --sidebar-primary: #8b5cf6;
   --sidebar-primary-foreground: #ffffff;
   --sidebar-accent: #18181b;
   --sidebar-accent-foreground: #d4d4d8;
   --sidebar-border: #27272a;
   --sidebar-ring: #8b5cf6;
   ```

4. Replace `.dark` variables:
   ```
   --background: #09090b;
   --foreground: #fafafa;
   --card: #18181b;
   --card-foreground: #f4f4f5;
   --popover: #27272a;
   --popover-foreground: #f4f4f5;
   --primary: #8b5cf6;
   --primary-foreground: #ffffff;
   --secondary: #27272a;
   --secondary-foreground: #a1a1aa;
   --muted: #27272a;
   --muted-foreground: #71717a;
   --accent: #27272a;
   --accent-foreground: #f4f4f5;
   --destructive: #ef4444;
   --border: #27272a;
   --input: #27272a;
   --ring: #8b5cf6;
   --chart-1: #8b5cf6;
   --chart-2: #a78bfa;
   --chart-3: #6d28d9;
   --chart-4: #c4b5fd;
   --chart-5: #ddd6fe;
   --sidebar: #020203;
   --sidebar-foreground: #d4d4d8;
   --sidebar-primary: #8b5cf6;
   --sidebar-primary-foreground: #ffffff;
   --sidebar-accent: #18181b;
   --sidebar-accent-foreground: #d4d4d8;
   --sidebar-border: #27272a;
   --sidebar-ring: #8b5cf6;
   ```

5. Update `@layer base` block:
   - Remove the `h1, h2, h3` font-family rule (no more --font-heading)
   - Update focus visible outline color from `--color-brand-500` to `var(--ring)`

6. Replace component classes:
   - `.btn-brand` → `.btn-primary`: use `bg-primary hover:bg-primary/90 text-primary-foreground`
   - `.btn-dark` → keep but update to use `bg-zinc-900`
   - `.btn-outline-brand` → `.btn-outline-primary`: use `border-primary text-primary hover:bg-primary hover:text-primary-foreground`
   - `.card-warm` → `.card-elevated`: `@apply bg-card rounded-xl p-6 border border-border; box-shadow: var(--shadow-card);`
   - `.card-photo` → `.card-surface`: `@apply bg-card rounded-xl overflow-hidden border border-border; box-shadow: var(--shadow-subtle);`
   - `.section-heading` → keep but remove font-family, just use `@apply text-3xl md:text-4xl font-bold tracking-tight`
   - `.section-subheading` → update to `@apply text-muted-foreground text-lg mt-2`
   - `.badge-tag` → remove font-family, use `@apply inline-block bg-muted text-muted-foreground text-xs tracking-wider uppercase px-3 py-1 rounded-full font-semibold`
   - Remove all `.dark .card-warm`, `.dark .card-photo`, `.dark .badge-tag`, `.dark .section-subheading` overrides — the new classes use semantic tokens that work in both modes

7. Update `.hot-pulse::before` gradient to use red/violet instead of red/terracotta:
   ```css
   background: linear-gradient(135deg, rgba(239, 68, 68, 0.15), rgba(139, 92, 246, 0.1));
   ```

8. Keep all animations (fadeIn, fadeInUp, etc.) and stagger classes — they're fine.

**layout.tsx — Font loading:**

1. Replace `Geist` import with `Inter`:
   ```tsx
   import { Inter } from "next/font/google";
   import { Geist_Mono } from "next/font/google";

   const inter = Inter({
     subsets: ["latin"],
     variable: "--font-inter",
     display: "swap",
   });
   ```

2. Update body className: `${inter.variable} ${geistMono.variable} antialiased`

3. Change `defaultTheme` from `"system"` to `"dark"` — dark mode is primary per user decision. Keep `enableSystem` so users CAN override.

**IMPORTANT:** After renaming CSS utility classes (card-warm → card-elevated, etc.), grep the entire codebase for old class names and update references. The executor MUST run: `grep -rn "card-warm\|card-photo\|badge-tag\|btn-brand\|btn-outline-brand\|btn-dark\|font-display\|font-heading\|font-body\|brand-500\|brand-600\|warm-50\|warm-100\|warm-200\|dark-950\|dark-700\|dark-400\|dark-300\|dark-200\|dark-100" app/src/` and update every file that uses old class names or color references to use the new semantic tokens (bg-background, text-foreground, bg-card, border-border, text-muted-foreground, etc.).
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - globals.css has violet/zinc palette with no terracotta references
    - Inter font loads via next/font, no Google Fonts CSS import
    - All old class names (card-warm, brand-500, warm-50, etc.) replaced throughout codebase
    - `npx next build` succeeds with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh navigation shell (sidebar, bottom nav, dashboard layout) and login page</name>
  <files>app/src/components/app-sidebar.tsx, app/src/components/bottom-nav.tsx, app/src/app/(dashboard)/layout.tsx, app/src/app/login/page.tsx</files>
  <action>
**DESIGN QUALITY NOTE:** Navigation is the constant UI element. It must feel polished on every page. Reference Linear's sidebar (dark, minimal, icon + label) and Vercel's mobile nav.

**dashboard layout.tsx:**
- Replace `bg-warm-50 dark:bg-dark-950` with `bg-background` (one semantic class works for both modes)
- Keep padding structure: `p-4 pb-20 md:p-8 md:pb-8`

**app-sidebar.tsx:**
- Update any hardcoded color references (brand-500, warm-*, dark-*) to semantic tokens
- Active item should use `text-primary` (violet) not `text-brand-500`
- Hover states: `hover:bg-accent` (uses --accent which is zinc-800 in dark)
- Ensure the sidebar header/logo area uses `text-foreground` not hardcoded colors
- Add subtle border-right: `border-r border-sidebar-border`

**bottom-nav.tsx:**
- Replace `border-warm-200/60 dark:border-dark-700/60` with `border-border`
- Replace `bg-white/90 dark:bg-dark-950/90` with `bg-background/90`
- Replace `text-brand-500` (active) with `text-primary`
- Replace `text-dark-400 hover:text-dark-600 dark:hover:text-dark-200` with `text-muted-foreground hover:text-foreground`
- Add a subtle active indicator dot below the active icon:
  ```tsx
  {isActive && <span className="absolute bottom-1 h-1 w-1 rounded-full bg-primary" />}
  ```
- Ensure each nav item has `min-h-[44px]` (already has this — verify)

**login/page.tsx:**
- Replace the entire page design with a premium dark aesthetic:
  - Full-page dark background (`bg-zinc-950` or `bg-[#09090b]`)
  - Centered card with subtle border (`border-border bg-card`)
  - "HouseFinder" title in large Inter font-bold, `text-foreground`
  - Subtle violet gradient accent line at top of card (4px tall, `bg-gradient-to-r from-violet-600 to-violet-400`)
  - Form inputs with `bg-input border-border` styling
  - Submit button with `bg-primary hover:bg-primary/90`
  - Remove any terracotta/desert references
  - Add a subtle tagline below the title: `text-muted-foreground text-sm`
  - Keep existing auth logic — only change the visual presentation
  </action>
  <verify>
    <automated>cd app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar shows violet accent on active item, zinc neutrals elsewhere
    - Bottom nav shows violet active state with indicator dot, 44px touch targets
    - Dashboard layout uses bg-background (no warm-50 or dark-950 references)
    - Login page has premium dark card design with violet accent
    - No references to brand-500, warm-*, dark-* colors in any modified file
  </done>
</task>

</tasks>

<verification>
- `grep -rn "brand-500\|warm-50\|warm-200\|dark-950\|dark-700" app/src/` returns zero matches
- `grep -n "Inter" app/src/app/layout.tsx` shows Inter font import
- `grep -n "8b5cf6\|7c3aed" app/src/app/globals.css` shows violet primary
- `npx next build` succeeds
- Dark mode renders with zinc/violet palette (no terracotta)
</verification>

<success_criteria>
Every page in the app renders with the new violet/zinc palette and Inter font. The old terracotta/desert aesthetic is completely gone. Login page looks premium. Navigation feels polished on both mobile and desktop.
</success_criteria>

<output>
After completion, create `.planning/phases/11-housefinder-ui-revamp/11-01-SUMMARY.md`
</output>
